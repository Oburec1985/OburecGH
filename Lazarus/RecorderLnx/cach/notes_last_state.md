# Текущее состояние проекта RecorderLnx

## Последние изменения (16.06.2026)

### 1. Исправление расчета порции отсчетов
- **Проблема:** Переменная `fDataUpdateMs` на форме настроек тегов инициализировалась после работы конструктора.
- **Решение:** Параметр `ADataUpdateMs` добавлен в конструктор `CreateDialog` класса `TTagSettingsDialog`.

### 2. Кроссплатформенный логгер в общем каталоге
- **Промпт:** `сделай так чоб было легко найти. Чтоб файлы лежали в одном каталоге но с разными названиями...`
- **Решение:**
  - В [uRecorderDebugLog.pas](file:///d:/works/OburecGH/Lazarus/RecorderLnx/Core/uRecorderDebugLog.pas) добавлена условная компиляция:
    - Под Windows: файл `LogWindows.log` пишется в корень проекта (на два уровня выше `lib\x86_64-win64\`).
    - Под Linux: файл `LogLinux.log` пишется в шару `/mnt/win_share/OburecGH/Lazarus/RecorderLnx/` (если она существует), что позволяет видеть его в Windows по тому же пути.

## На чем остановлена работа
- Проект скомпилирован.
- После запуска программ под Windows и Linux оба лога `LogWindows.log` и `LogLinux.log` должны появиться в корневой папке проекта: `D:\works\OburecGH\Lazarus\RecorderLnx\`.
