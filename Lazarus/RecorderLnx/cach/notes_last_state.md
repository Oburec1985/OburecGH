# Статус проекта RecorderLnx

## Последние изменения (17.06.2026)

### 1. Настройка по двойному клику
- **Задача:** Сделать редактирование/настройку узла устройства по двойному клику в дереве оборудования на вкладке "Аппаратные свойства".
- **Решение:** Изменить метод `TRecorderSettingsDialog.fHardwareTreeDblClick`. Если `Data` выбранного узла равна `nil`, и при этом узел является дочерним к корневому (узел устройства, а не корень "Устройства"), вызывать `HardwareEditSourceClick`.

## Текущий статус
- Подготовлен план реализации. Ждем подтверждения пользователя для начала выполнения.
