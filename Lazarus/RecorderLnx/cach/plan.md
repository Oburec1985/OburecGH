# План доработки MIC-140

## Плагины и Lua-расчёты (18.09.2026)

- [x] Ускорить отрисовку таблицы тегов в настройках за счёт кэша иконок.
- [x] Показать плагин осциллограммы как «Осциллограмма» без изменения имени DLL.
- [x] Поддержать `{Выход} = {Вход}.Value` и предлагать создать отсутствующие виртуальные теги при выходе из редактора.
- [x] Открывать редактор двойным щелчком по строке LuaCalcPlugin.
- [x] Установить Lua 5.4 x64 в локальную Windows-сборку и проверить повторный расчёт в отдельном smoke-запуске.
- [ ] После закрытия RecorderLnx пересобрать штатные DLL/EXE и проверить GUI: задержку перетаскивания и LuaTest при изменении E1/E2.
- [ ] Позже создать инсталляторы Lua-плагина для Windows и Linux, включая библиотеку Lua и проверку архитектуры.

## Плагин расчётных Lua-скриптов (18.09.2026)

- [x] Очистить группу проектов от вспомогательных проб и инструментов, оставить RecorderLnx, осциллограмму и новый Lua-плагин.
- [x] Добавить редактор подпрограмм и список тегов с вставкой `{имя}.Value`.
- [x] Добавить ABI чтения тегов, записи виртуальных тегов и пути проекта; собрать RecorderLnx и LuaCalcPlugin.
- [ ] Проверить выполнение на Windows с Lua 5.3/5.4 x64 и на Linux с системной Lua.

## Легенда и состояние кнопок осциллограммы (18.09.2026)

- [x] Показывать актуальное значение каждой линии в легенде; для X1/X2 — показания и разность.
- [x] Обновлять легенду при новом кадре и перемещении курсоров без пересоздания контролов.
- [x] Выделять нажатые кнопки панели цветным фоном.
- [x] Собрать штатный Windows RecorderLnx.
- [ ] Проверить на живом сигнале движение курсоров и обновление легенды.

## Панель осциллограммы и значащие цифры (18.09.2026)

- [x] Вынести форматирование до четырёх значащих цифр в SharedUtils и применить к панели/осям.
- [x] Добавить кнопки курсоров, автодиапазона и легенды; сохранить режимы в проекте.
- [x] Убрать выбор источника триггера с главной панели, оставив его в настройках.
- [x] Проверить полную Windows-сборку в отдельный EXE.
- [ ] Закрыть запущенный RecorderLnx и пересобрать штатный EXE; проверить управление в живом GUI.

## Linux-плагин осциллограммы (18.09.2026)

- [x] Пересобрать RecorderLnx и SampleInfoPlugin в Linux VM в одном каталоге `lib/x86_64-linux`.
- [x] Проверить загрузку `.so` пробой жизненного цикла и чтением метаданных из `plugins`.
- [ ] При необходимости установки на KIP отдельно сформировать пакет и развернуть его; текущий запрос касается локальной сборки.

## Цвет осей осциллограммы (18.09.2026)

- [x] Назначать оси цвет первого видимого сигнала на ней; для пустой оси оставить нейтральный цвет.
- [x] Проверить полную Windows-компиляцию в отдельный EXE.
- [ ] После закрытия работающего RecorderLnx пересобрать штатный EXE и проверить цвета на живом графике.

## Поле ввода на Linux (18.09.2026)

- [x] Убрать имя компонента из пустого поля в режиме редактирования; принимать только числовой ввод со знаком и одним разделителем.
- [x] При записи значения приравнивать точку и запятую к локальному десятичному разделителю.
- [ ] После закрытия запущенного Windows RecorderLnx пересобрать штатный EXE и проверить ввод/пустое поле в GUI.
- [x] Передавать drag редактору через существующий контейнер в edit mode.
- [x] Проверить полную Windows-компиляцию в отдельный EXE.
- [x] Собрать штатный Windows EXE после закрытия приложения.
- [x] Собрать Linux ELF в VM.
- [ ] После остановки в `HandleNeeded` повторно проверить клик, перетаскивание,
      ресайз и открытие настроек на Linux GTK.

## Дубли тегов и адрес источника (17.09.2026)

- [x] Сверить исходный ODS с экспортом и подтвердить причину пропуска 64 каналов KIP-3.
- [x] При загрузке оставить один физический тег на SourceId+Address, временно разрешать старые ID/имена в него до пересохранения GUI.
- [x] Добавить строковый адрес источника в модель, дерево устройств и диалог; каскадно менять адреса тегов MIC-185 и MC-032 с проверкой коллизий.
- [x] Сохранить пользовательский префикс MIC-185 при загрузке и в рантайме.
- [x] Собрать Windows/Linux и развернуть 0.1.44 на KIP-1…4.
- [ ] Подтвердить запуск GUI на KIP-3 с дублями; затем оператору импортировать ODS и пересохранить конфиг.
- [ ] Адаптировать драйвер MIC-140 к редактируемому строковому адресу (пока явный отказ).

## Главное окно при запуске (17.09.2026)

- [x] Задать главной форме начальное состояние `wsMaximized`.
- [x] Увеличить версию и проверить принудительную Windows-сборку.
- [ ] При следующем Linux-релизе проверить запуск на KIP визуально.

## Осциллограмма разных источников (17.09.2026)

- [x] Сохранять полный временной буфер, а после триггера показывать выбранный мышью участок шириной окна с предтриггерным смещением; ось видимого участка — 0..ширина.
- [x] Добавить настройку смещения до триггера в процентах видимого окна и сохранение в проекте.
- [ ] Проверить на живом сигнале мышиный зум 300→20 мс, смещение 5 мс, ось 0..20 мс и повторный триггер.
- [x] При включённом триггере обновлять кадр только после нового восходящего пересечения выбранного канала и заполнения окна; удерживать предыдущий кадр без события.
- [ ] В GUI проверить удержание кадра, длительный высокий уровень, повторный фронт после развёртки и отдельный триггерный канал.
- [x] Убрать ограничение одного SourceId и совместить линии по timestamp каждого отсчёта в общем окне.
- [x] Добавить назначение Y-оси для канала, сохранение осей и показ настроек выбранной оси.
- [x] Добавить spin-кнопки развёртки/амплитуды, фиксировать Y-диапазон между обновлениями.
- [x] Добавить горизонтальный курсор уровня триггера и закрытый вход без изменения буфера тега.
- [x] Проверить forced Win64-сборку в отдельный EXE и тест сохранения модели.
- [ ] Повторить штатную сборку после закрытия запущенного RecorderLnx.exe; проверить GUI на нескольких устройствах и Linux-сборку.

## Бублик на мнемосхеме (17.09.2026)

- [x] Добавить тип компонента, палитру, выбор нескольких тегов и отрисовку BGRA Controls.
- [x] Сохранять настройки и ID тегов в проекте, переносить при копировании.
- [x] Проверить полную Windows-сборку RecorderLnx.
- [x] Перенести круговую гистограмму и цифровой индикатор в группу «Индикаторы».
- [x] Добавить режим одного тега с диапазоном и отдельную пиктограмму в командный ImageList.
- [x] Пересобрать штатный EXE после закрытия запущенного приложения.
- [ ] Проверить внешний вид и выбор тегов в GUI на Windows и Linux.

## Экспорт исторического SQL-тренда (17.09.2026)

- [x] Выбирать каналы и UTC-интервал; сохранять и загружать список каналов.
- [x] Экспортировать CSV с общей осью X, интерполяцией между точками и `-`
  при отсутствии данных.
- [x] Экспортировать комплект WinПОС/MERA штатным writer с реальными точками.
- [ ] Проверить открытие экспортированного комплекта в установленном WinПОС
  и визуально проверить диалог на Windows/Linux.


## Импорт общей таблицы каналов (16.09.2026)

- [x] Сопоставлять сперва по имени, затем по источнику и адресу; при отсутствии обоих пропускать. Пустое имя канала пропускать целиком.
- [x] Не использовать ID при импорте; новые ID делать случайными, существующие сохранять.
- [x] Ускорить импорт индексами тегов, однократной проверкой повторов и резервированием массива строк.
- [x] Собрать Linux-пакет 0.1.35 и установить на KIP-1…KIP-4 (4/4 успешно).
- [x] Исправить регрессию сортированного индекса и установить пакет 0.1.36 на KIP-1…KIP-4 (4/4 успешно).
- [ ] Проверить повторный импорт на KIP-4; ранее испорченное имя `40-11` восстановить отдельно вручную или из резервной конфигурации (пустая ячейка ODS его не восстановит).

# Плагины RecorderLnx: поэтапная совместимость

- [x] Сверить `PLUGININFO` и экспорт `GetPluginInfo` с оригинальным Recorder и
  `plgEvalFRF`.
- [x] Читать информацию из DLL/so каталога `plugins` рядом с EXE и показывать
  её во вкладке настроек без создания экземпляра плагина.
- [x] Добавить общий SDK и пример Lazarus DLL/so, включить его в группы
  проектов и проверить загрузку в Windows/Linux.
- [x] Добавить кнопки добавления/удаления и снимок метаданных без повторного
  чтения DLL/so при открытии настроек.
- [x] Перенести реестр подключённых плагинов из проектного `plugins.ini` в
  глобальный `app.ini`: запись/удаление сохранять сразу, независимо от
  «Применить»/«OK» и сохранения проекта; предусмотреть однократную миграцию
  существующего проектного списка.
- [x] Исправить перекрытие кнопок списком: вынести компоновку вкладки в `.lfm`.
- [ ] Визуально подтвердить кнопки на пользовательском запуске версии 0.1.28.
- [x] Добавить переносимый lifecycle/host API для `CreatePluginClass`,
  уведомлений и фабрики тестовой осциллограммы без зависимости от COM.
- [x] Показывать отдельно сохранённую и фактически загруженную версию плагина,
  полный путь DLL/so; писать активную версию и путь в журнал загрузки.
- [x] Добавить плагинной осциллограмме встроенную панель XScale/YScale/Offset,
  источника, флага и уровня триггера; штатный компонент не менять.
- [ ] Визуально проверить панель на уже сохранённом компоненте и на новом.
- [ ] Расширить API доступа к тегам и настройкам для реальных плагинов;
  проверить Linux-сборку и визуальное создание компонента в GUI.
- [x] Для отсутствующего удалённого Firebird пропускать `Open` SQL-тренда и
  передавать одно сообщение в журнал RecorderLnx без отладочного исключения.
- [ ] Проверить на свежем GUI потерю и восстановление SQL-связи после закрытия
  удерживающего старый EXE процесса.

## LinuxSetupManager: интерфейс системных настроек (16.09.2026)

- [x] Показывать диски таблицей при входе, безопасно обрабатывать `null` lsblk.
- [x] Автоматически предлагать точку монтирования и показывать выбор ФС.
- [x] Собрать DEB 0.1.24 и установить на KIP-1…KIP-4 без `.200`.
- [ ] Проверить установленный диалог дисков глазами на каждом типе устройства.
- [x] Убрать блокирующий опрос дисков из открытия формы; собрать DEB 0.1.25 и
  установить на KIP-1…KIP-4.
- [ ] Подтвердить открытие окна и обновление занятости на KIP с большим NTFS.
- [x] Разделить физические накопители и разделы/LVM в форме; расширить таблицы
  и включить перестановку GTK2-колонок.
- [x] После окончания параллельной правки собрать пакет 0.1.26 и установить
  на KIP-1…KIP-4 без `.200`.
- [ ] Визуально проверить два списка и перестановку колонок на KIP.
- [x] Исправить обновление разделов при выборе другого физического диска и
  заменить две неподвижные голубые границы настоящими `TSplitter`.
- [x] Установить DEB 0.1.27 на KIP после восстановления SSH/22: 4/4.
- [ ] На KIP проверить переключение обоих дисков и перетаскивание двух
  разделителей в GTK2.
- [x] Различить горизонтальные полосы прокрутки и разделители по снимку;
  добавить недостающий разделитель между списком разделов и сведениями,
  установить DEB 0.1.30 на KIP-1…KIP-4.
- [ ] Проверить перетаскивание новой ручки между разделами и сведениями в GTK2.

- [x] Пояснить, что «Предпросмотр» показывает изменения без их применения.
- [x] Выбирать установленную программу из дерева категорий `.desktop`.
- [x] Показать пример формата NTP-серверов рядом с полем ввода.
- [ ] Проверить дерево вручную в Astra GUI и включить проверенную сборку в пакет.

- [x] Перенести начальную настройку новых визуальных компонентов из MainForm в
  целевые фабрики; оставить в форме единый сценарий размещения.
- [x] Применять явное пакетное включение аппаратных ГХ ко всем выбранным тегам.

- [x] Загружать признак записи каналов в SQL из колонки `SQLdb` таблицы,
  опционально отключать неотмеченные каналы и удалять их данные из БД.

- [x] Сверить Reset MIC-185 с оригинальным Recorder и убрать лишнюю временную
  cleanup-сессию между Disconnect и штатным Connect/Program.

- [x] Помечать серым отсутствующие и вышедшие за диапазон входы e1/e2/e3;
  не показывать и помечать серым неприменимые S1/S2.

- [x] В подписи максимального напряжения измерительного сечения показывать,
  по какой компоненте найден максимум: `S1` или `S2`.

- [x] Добавить двусторонний компонент «Поле ввода» с выбором только тегов,
  которым явно разрешён внешний ввод.

- [x] Хранить импортированные из БДГХ характеристики ссылкой `sdbKey`, не
  копировать их точки в проект; автономные и отредактированные ГХ не менять.

- [x] Показывать настроенный угол точки измерительного сечения даже при
  отсутствии данных связанных тегов.

- [x] Добавить измерительному сечению пакетный импорт: один файл распределять
  по всем компонентам фабрики согласно ID «Объект/Сечение».

- [x] Запрашивать фактический каталог Mera Files каждого RecorderLnx отдельной
  командой `config.get` и не передавать конфигурационные пути в heartbeat.

- [x] Добавить переносимый Desktop-ярлык для запуска настройки Samba двойным
  щелчком с системным запросом прав администратора.

- [x] Добавить в RCPanel выбор основного RecorderLnx и фоновую синхронизацию
  дерева `SDB` через общий SMB-ресурс `MeraFiles`; подготовить Samba-скрипт.

- [x] Гарантировать реальную синхронизацию Firebird `SYSDBA/123`: не доверять
  password-файлу, менять пароль через `gsec` и проверять повторным входом.

- [x] Не допускать SQL `Open` до полной сборки конфигурации; нормализовать
  внутренние Firebird credentials после legacy/app config merge.

- [x] Разделить системное подключение SQL (`app.ini`) и переносимые проектные
  параметры/каналы (`sql-db.ini`); пересобрать Win64 и все Linux-цели.

- [x] Добавить rcPanel постоянный реестр хостов, ICMP, немедленный LAN-поиск,
  Wake-on-LAN и команды фоновому RecorderHostAgent для запуска RecorderLnx и
  разрешённого выключения ПК. Инвентарь хоста (MAC, ОС и путь EXE) передаёт
  сам RecorderLnx через штатные hello/heartbeat; HostAgent его не дублирует.

- [x] Открывать импорт/экспорт таблицы тегов в родительском каталоге текущей
  конфигурации проекта.

- [x] Свернуть ожидаемые отсутствующие каналы общей таблицы импорта тегов в
  один счётчик и ограничить подробный список настоящих предупреждений.

- [x] Добавить двустороннее UDP Broadcast-обнаружение RecorderLnx и
  Coordinator/rcPanel с переходом на штатный HTTP endpoint.

- [x] Объединить Осциллограмму, Тренд, SQL-тренд и Спектр в общую кнопку
  «Графики» с выпадающим меню и иконками в тулбаре компонентов.

- [x] Сделать lifecycle Coordinator надёжным: дисковая FIFO-очередь,
  восстановление после перезапуска, корректный UTC и файловый лог всех стадий
  HTTP/grouping/SQL в RecorderCoordinator.
- [x] Добавить SQL-тренду сохраняемое отображение событий и курсор-руку при
  наведении на интерактивный маркер.

- [x] Добавить полноценную карточку SQL-события: собственные имя, описание и
  время, пакеты/хосты/пути, редактирование путей и безопасное удаление SQL-записей;
  в Coordinator добавить просмотр событий за выбранный интервал.
- [x] Добавить в список событий Coordinator отдельные команды открытия всех
  доступных MERA-замеров и безопасного удаления завершённого события.

- [x] Передавать Coordinator фактические имя и путь MERA-замера вместо
  `Запись RecorderLnx`/`record.mera`; в SQL-тренде показывать подпись только
  для явно выбранного события и снимать выбор щелчком по свободному месту.

- [x] Восстановить оригинальную временную ось MIC-185: аппаратный
  `sampl_count/Fs`, поканальный scan offset, разрывы и wrap; использовать общий
  контракт времени AcquisitionBlock с UTS и без UTS.

- [x] Читать и калибровать все TIn термокомпенсации без пользовательских
  тегов, не публиковать скрытые каналы и предупреждать об отсутствующих ГХ.

- [x] Исправить runtime-переключение аппаратной ГХ MIC-140: флаг принадлежит
  тегу, ГХ применяется к следующему блоку без пересоздания источника.

- [x] Добавить в общий диалог автопоиска checkbox выделения/снятия
  всех новых устройств, не затрагивая уже добавленные.

- [x] Разделить у MIC-140 физическую единицу источника (`mV/degC`),
  флаг аппаратной ГХ тега и автоподбор единиц; MIC-185 не менять.

- [x] Добавить кнопке Winpos контекстный выбор: открыть каталог или
  запустить Winpos, с сохранением выбора в run-control.ini.

- [x] Связать выбор компенсатора MIC-185 с аппаратным `GroupAddition`: четыре
  независимые группы по 16 каналов, в каждой вход 1..4 либо «выкл».

- [x] Вынести настройку тензо-ГХ в общий frame, встроить его в диалог и БДГХ,
  сохранять правки JSON по исходному ключу; импортировать ГХ без пути в имени.

- [x] Корректно отображать выбранную тензокалькуляторную ГХ в БДГХ:
  параметры, runtime-формулу, рабочий диапазон и вычисляемый график.

- [x] Разрешить хранение тензокалькуляторной ГХ в БДГХ: JSON v1 сохраняет
  полный объект ГХ и загружается тем же деревом без табличной аппроксимации.

- [x] Добавить в дерево БДГХ контекстную команду создания каталога с
  проверкой имени, записью descriptor XML и обновлением выбора.

- [x] Сделать транзакционное редактирование канальной ГХ из настроек тега:
  общая правка либо локальная копия; кнопку-весы перевести на экспорт в
  выбранную папку БДГХ.

- [x] Убрать дублирование флага и имени аппаратной ГХ MIC-185 из `dataSource`;
  сохранять и загружать эту настройку только как свойство тега.

- [x] Добавить opt-in режим `--capture-user-guide`, который после старта
  последовательно снимает пользовательские формы, нумерует активные элементы
  на PNG и сохраняет карты элементов в `Docs/Руководство пользователя/screens`.
- [x] Подготовить русское руководство пользователя RecorderLnx в Markdown со
  снимками и таблицами элементов форм.

- [x] Добавить для измерительного сечения настраиваемые фон компонента и фон
  текста измерений с выбором цвета двойным щелчком и сохранением в проекте.

- [x] Исключить откат размера, когда заголовок и напряжения сечения используют
  одно имя шрифта, и корректно применить его ко всем сечениям фабрики.

- [x] Исправить импорт измерительного сечения из LibreOffice ODS с пустым
  повторяющимся хвостом до столбца 16383 и показывать итог импорта.

- [x] Перенести диалог настройки измерительного сечения в редактируемую LFM-форму
  и добавить назначение выбранных шрифтов всем сечениям фабрики.

- [x] Подключить измерительное сечение к общему менеджеру шрифтов отдельно для
  заголовка и механических напряжений; переносить длинные надписи по ширине.

- [x] Пакетный Ctrl/Shift-сброс выбранных приборов выполняет независимые
  жизненные циклы параллельно и сохраняет multi-selection при открытии меню.

- [x] Добавить открепляемые окна пользовательских формуляров с собственным тулбаром,
  сохранением положения, размера, развёрнутого состояния и выбранного монитора.

- [x] Добавить сохраняемое включение/отключение узла источника из контекстного
  меню дерева; отключённые узлы не создают runtime-источники и не собирают данные.

- [x] Не перепрограммировать MIC-140 после `OK` в настройках тега, если
  аппаратная сигнатура источника не изменилась.

- [x] Общий автопоиск аппаратуры показывает MIC-140, MIC183/185 и MC-032
  единым списком с checkbox; уже настроенные источники по умолчанию не отмечены.
- [x] Автопоиск использует выбранный сетевой адаптер: сначала штатные broadcast
  протоколы без повторного TestLink, затем TCP/4000 и протокольную идентификацию
  по фактической маске подсети.

- [x] Добавить на пользовательские редактируемые страницы настраиваемое
  фоновое изображение с сохранением переносимого пути в GUI-конфигурации.

- [x] SourceId — свойство/`BuildSourceId`; для MIC-140 всегда из host:port (не константа, расходящаяся с mic140.host).
- [x] Изменить `uRecorderMic140DataSource.pas` для перехода на синхронный опрос в `DoTick` и последовательный жизненный цикл.
- [x] Собрать и проверить проект `RecorderLnx`.
- [x] Проверить сборку тестового проекта `Mic140ProtocolDebug_Codex`.
- [x] ~~Разделить SourceId и IP, оставив SourceId константным~~ — отменено: SourceId вычисляется из host (см. выше).
- [x] Надежно исправить ошибку Access Violation при смене IP-адреса через безопасную процедуру переименования с отключением `OwnsObjects`.
- [x] Исправить задвоение доступных каналов и перепривязать теги при смене IP-адреса MIC-140.
- [x] Разделить MIC-140 Stop и Disconnect; сохранить Init/Configure между
  последовательными Preview и проверить двумя циклами в одном процессе.

- [x] Не сбрасывать Fs MIC-185 в 100 Гц при `Применить` аппаратных настроек.
- [x] Не переводить соседние live-приборы в постоянный `offline` диагностическим
  `TestLink` при входе в общие настройки.
- [x] Пересчитывать автоматическую порцию тега и логический блок буфера через
  `ceil(Fs * DataUpdateMs / 1000)` при изменении Fs или периода данных.

# План SQL-событий MERA и серверного архива (04.09.2026)

- [x] Исправить Firebird schema v4: не включать полный `relative_name` в
  составной PK; проверить миграцию 3→4 и повторный запуск на реальной БД.
- [x] Сшивать ручной локальный адрес с UUID авто-регистрации RecorderLnx;
  отображать offline/stop/preview/record цветом и дать групповые и адресные команды.
- [x] Сделать TCP preflight клиента Coordinator HTTP-корректным: отправлять
  request line и заголовки до закрытия соединения.
- [x] Запускать RecorderCoordinator без параметров в GUI-режиме; разрешить
  ручное добавление сохраняемых хостов и автоматический приём клиентов.
- [x] Перенести постоянные роли AI-команды RecorderLnx в текущую задачу.
- [x] Провести совместный аудит текущих schema v3, MERA lifecycle и SQL trend.
- [x] Зафиксировать ADR schema v4 и границу `RecorderArchiveService`.
- [x] Предусмотреть двунаправленный сетевой клиент RecorderLnx и протокол
  управления/конфигурации с Coordinator.
- [x] Добавить в контракт запрос текущего/последнего замера, исходного пути,
  устойчивого URI, manifest и фактической доступности.
- [x] v4a: общие DTO/envelope и protocol compatibility tests.
- [x] v4b: client state machine, heartbeat/reconnect и mock server.
- [x] v4c: безопасный command dispatcher `state/prepare/start_at/stop`.
- [x] v4d: добавить schema/repository API и миграционные тесты.
- [ ] v4e: регистрировать manifest законченного локального MERA-пакета.
- [x] v4f: показать read-only маркеры, hover и pin в SQL-тренде.
- [x] v4g: добавить диалог и локальное открытие выбранных пакетов в WinПОС.
- [ ] v4h: реализовать серверный upload/download и reconciler.
- [ ] v4i: открывать архивный пакет через проверенный локальный cache.

# Итерация 2026-09-04: MERA timestamp и отвязка тегов

- [x] Сверить дату/время `.mera` с оригинальным Recorder.
- [x] Записывать локальные `Time` и `Date` начала кадра.
- [x] Вернуть отвязывание тегов при удалении источника.
- [x] Сохранять `Detached:`-теги между перезапусками.
- [x] Пересобрать RecorderLnx и прогнать существующий тест MERA storage.

# Итерация 2026-09-09: события записи координатора

- [x] Добавить сохраняемую галочку создания SQL-событий записи.
- [x] Сохранять общий event и отдельные MERA-пакеты каждого хоста.
- [x] Группировать любые фактические старты по фиксированному временному окну,
  независимо от команды запуска и correlation ID.
- [x] Исключить привязку `recording.completed` по временному окну.
- [x] Передавать из Recorder времена, проект и физический entry path.
- [x] Открывать пакет SQL-тренда по `data_file_locations.path_key` системным
  запуском файла.
- [x] Собрать Coordinator и выполнить тест группировки lifecycle.
- [ ] Выполнить полный SQL roundtrip на стенде с доступной Firebird/SQLite и
  реальными путями нескольких Recorder-хостов.
- [x] Исправить несовместимый с FPC SQLDB тип `INT128` в агрегатах размеров
  событий и пакетов; проверить оба запроса на реальной Firebird.

# Итерация 2026-09-15: настройки и фабричная палитра компонентов

- [x] Устранить AV при открытии настроек из-за отсутствующего LFM-контрола.
- [x] Скрыть публикацию каталога под Windows, сохранив её под Linux.
- [x] Выполнить forced Windows/Linux build, собрать DEB и развернуть 0.1.18 на
  четырёх Linux-хостах.
- [x] Строить палитру компонентов из метаданных зарегистрированных фабрик.
- [x] Убрать из MainForm перечисление типов и отдельные обработчики выбора.

# Итерация 2026-09-17: Linux build и deploy на KIP

- [x] Собрать RecorderLnx и сопутствующие проекты под Linux с принудительной перекомпиляцией.
- [x] Собрать DEB версии 0.1.41 и проверить зависимости ELF.
- [x] Проверить отсутствие активных файлов записи на KIP-1…4 перед установкой.
- [x] Установить DEB на KIP-1…4; пакет 0.1.41 подтверждён на каждом.
- [x] Восстановить GUI через HostAgent после пакетного обновления и проверить живые процессы на всех четырёх KIP.
- [ ] Проверить GUI осциллограммы и поведение при реальных данных на KIP.
