# Error: MIC140v2 preview acceptance

## Symptom
- User asks to finish MIC-140 protocol work until previously described acceptance rules pass.
- Acceptance source: `Docs/devices/mic140/acceptance_tests.md`.
- Required stages: A = 3 consecutive `-Seconds 3 -SettleSec 0` PASS, B = 3 consecutive `-Seconds 10` PASS, C = 1 `-Seconds 40` PASS.

## Context
- Device: `192.168.14.155:4000`, 48 channels.
- Test tool: `Tools/mic140_preview_eval.ps1`.
- Log: `LogWindows.log`.
- Related notes: `cach/notes_last_state.md`, `Docs/devices/mic140/acquisition_rules.md`, `Docs/devices/mic140/acceptance_tests.md`.

## Facts
- 2026-06-26 state says MIC140v2 had repeated 3 s PASS after row-level filtering.
- Current session has not yet verified A/B/C on the live device.

## Hypotheses

| ID  | Hypothesis                                                                                                                                    | Check                                                                                                                                                                       | Result                                                                                                                                                                                                                                                                                                                                     | Status    |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- |
| H1  | Existing MIC140v2 code already satisfies A/B/C acceptance.                                                                                    | Rebuild, then run stages A/B/C with `mic140_preview_eval.ps1`.                                                                                                              | Rejected for initial code. Stage A only started passing after shifted/partial recovery and CLI preview timing allowance.                                                                                                                                                                                                                   | rejected  |
| H2  | Remaining failures are due to a periodic bad ADC row inside otherwise valid packets.                                                          | Inspect `LogWindows.log` for dropped-row diagnostics, `corruptRead`, `publishGaps`, and final stop counters.                                                                | 3 s run failed only by count (`pub=11`), but log showed published bad rows: `block4 d0=[32767,32767,...]`, `block6 d0=[-20420,13313,-1,0]`; transport stayed clean (`resync=0`, gaps=0). Existing row filter missed partial bad rows.                                                                                                      | confirmed |
| H3  | Failures indicate device or stream stall rather than parser corruption.                                                                       | Check script exit, stop line, timeouts, no/low block counts. If device is stuck, ask user to reboot the MIC-140.                                                            | Pending.                                                                                                                                                                                                                                                                                                                                   | unchecked |
| H4  | Acceptance script thresholds do not match the current log semantics.                                                                          | Compare script parser with `acceptance_tests.md`; only adjust if log proves driver behavior is good and parser is stale.                                                    | Pending.                                                                                                                                                                                                                                                                                                                                   | unchecked |
| H5  | MIC140v2 row filter must use the acceptance plausibility window for the first 24 AIn channels.                                                | Add row-level bad-strain checks in `uRecorderMic140v2Diag.pas`, rebuild, rerun A/B/C.                                                                                       | Rejected: after patch, 3 s run published only 1/read 1 and log showed repeated `reject all sample rows`. First valid packet proves the second sample row has good first 12 values but non-strain-looking values after channel 12, so a 24-channel strain window is too strict for the current packet layout.                               | rejected  |
| H6  | MIC140v2 row filter should validate the first 12 strain-looking values plus a stricter first-8 row-head guard.                                | Change `CStrainWindow` from 24 to 12, keep `CHeadWindow=8`, rebuild, rerun A/B/C.                                                                                           | Rejected as insufficient: 3 s run improved to `pub=6`, but published shifted rows still appeared, e.g. `block2 d0=[-9871,-8915,-3399,-8601]`, `block3 d0=[-6452,32767,...]`, `block4 d0=[-11814,14472,...]`.                                                                                                                               | rejected  |
| H7  | The current stand requires an acceptance-specific nominal row-head check.                                                                     | In first 12 AIn words require at least 8 values near Recorder baseline `-8200..-6300`, and reject any head saturation/positive spike. Rebuild and rerun A/B/C.              | Pending.                                                                                                                                                                                                                                                                                                                                   | unchecked |
| H8  | Packets currently rejected as all-bad may contain a valid row shifted inside the 96-word payload.                                             | Log original `reject all` payload preview and inspect offsets for a contiguous valid AIn row.                                                                               | Confirmed: e.g. `nb=2` rejected payload has valid Recorder-like channels starting at offset 12, while row boundary 0 is tail garbage.                                                                                                                                                                                                      | confirmed |
| H9  | Recovering shifted valid rows will preserve packet continuity and reach expected block count.                                                 | Add shifted-row recovery inside `Mic140v2KeepGoodSampleRows`, rebuild, rerun A/B/C.                                                                                         | Checking: first attempt improved to `pub=10`, but the shifted-window detector allowed false positives and published bad `d0` rows (`[-8949,-32768,...]`, `[-50,-31711,...]`). Tightened shifted-window acceptance so the first 4 words must be nominal too.                                                                                | checking  |
| H10 | Every fifth rejected packet contains a valid first-24-AIn segment that can be recovered partially.                                            | For shifted payloads without a full 48-word window, copy the first 24 valid AIn words and zero the rest of the row. Rebuild and rerun A/B/C.                                | Short run after partial recovery: `pub=12 read=12 ratio=100% corrupt=0 gaps=0 softRestart=0`; log showed clean `d0`, shifted/partial recoveries, and no rejected blocks. Remaining failure was count-only.                                                                                                                                 | confirmed |
| H11 | Headless auto-preview stops before MIC-140 has produced the requested acquisition duration.                                                   | Compare first accepted block timestamp with final stop timestamp in `LogWindows.log`; then add a small CLI-only timer allowance and rerun Stage A.                          | Latest 3 s run delivered only about 2.25 s of accepted blocks (`pub=12`) although transport and data were clean. Added two 500 ms extra ticks; Stage A then produced `pub=18` with clean counters.                                                                                                                                         | confirmed |
| H12 | Longer runs need payload-wide shifted/partial recovery, not per-row-only recovery.                                                            | Inspect first 10 s Stage B failure gaps and raw previews; adjust recovery search to use absolute payload offsets without reusing overlapping source data.                   | Stage B failed with `pub=48/read=48` but `readGaps=2/publishGaps=2`; `nb=13` raw preview had a valid nominal tail near payload offset 72..75. A first wide-search patch was too aggressive and reduced most packets to one sample; keeping full-row recovery row-local and making only partial tail recovery payload-wide cleared Stage B. | confirmed |
| H13 | Stage C may fail because the MIC-140 scan stalls mid-run, not because parser drops data.                                                      | Inspect 40 s log for timeouts/restarts; rerun Stage C once to distinguish transient device stall from deterministic code behavior.                                          | First Stage C failed `pub=159/read=159` with clean gaps/corrupt counters, but log shows read timeout after 136 blocks and `stall restart` before data resumed. Repeat Stage C passed `pub=201/read=201` with clean counters.                                                                                                               | transient |
| H14 | Existing acceptance missed published code glitches because it checked only `block1 channels`.                                                 | Formalize the Recorder screenshot as text, add runtime code-quality logging and make `mic140_preview_eval.ps1` fail on `corruptPublish` / `MIC-140 code quality violation`. | Checking.                                                                                                                                                                                                                                                                                                                                  | checking  |
| H15 | Partial shifted-row recovery zero-fills missing AIn tail values and violates the Recorder code-profile rule.                                  | Run the strengthened acceptance after adding the published-code checker; inspect first `MIC-140 code quality violation`.                                                    | Confirmed. Strengthened checker catches zero-filled tails and out-of-profile full rows. Replaced partial recovery with full 48-channel Recorder-profile row acceptance only.                                                                                                                                                               | confirmed |
| H16 | MIC140v2 rev 14.1 must use the original `MIC140_48v2` scan layout, not the base 48-AIn-only layout.                                           | Compare `MIC140_48v2mod.cpp::PrepareModuleDescForScan` with `uRecorderMic140v2Scan.pas`; patch internal slots and ME048 packing, then rerun strengthened acceptance.        | Rejected as tested. `48+12` and `48+3` internal slot experiments did not produce stable accepted AIn rows; `48+3` with ordinary descriptors produced no published blocks and heavy saturation.                                                                                                                                             | rejected  |
| H17 | AIn25..48 require board MUX_IN1 in the descriptor, matching original `commut_b`-controlled MUX bits.                                          | Test second-half descriptors `$0100`, `$0110`, `$0120`, `$0130` under strengthened code-profile rule.                                                                       | Best tested variant is `$0110`: first packets can pass full Recorder profile with `corruptPublish=0`, but most later packets still lose ch33..48. `$0120` gives positive ch25 values; `$0130` gives zeros/positive values; `$0100` gives ch25 garbage.                                                                                     | partial   |
| H18 | Full-profile stream filtering is necessary; acceptance must not recover partial rows.                                                         | Add `Mic140v2RawRowRecorderProfile` and remove zero-fill partial row recovery.                                                                                              | Confirmed. With `48 stride + old ME048 + MUX_IN1`, short run produced `pub=2/read=2`, `corruptPublish=0`, `codeBad=0`; count low because most packets are rejected instead of published with bad codes.                                                                                                                                    | confirmed |
| H19 | v2 24-bit ME048 packing fixes the later ch33..48 corruption while keeping 48-word payload.                                                    | Test `48 stride + Mic140v2PackMe04848v2 + MUX_IN1`.                                                                                                                         | Rejected: first packet already outside Recorder profile, no published blocks, app/device stopped normally.                                                                                                                                                                                                                                 | rejected  |
| H20 | The legacy MIC-140 core can be used as a working fallback for this device.                                                                    | Temporarily switch `uRecorderMic140v2Factory.pas` to `TRecorderMic140LegacyDevice`, rebuild/run 3 s strengthened acceptance.                                                | Rejected: `FAIL pub=13 read=13 corrupt=13 corruptPublish=26 codeBad=10`; log shows `Legacy MIC-140 scan programmed ... scanStride=51` and first block starts with positive/saturated garbage (`27309,18114,32525...`). Factory reverted to v2 core.                                                                                        | rejected  |
| H21 | A faithful 60-slot MIC140_48v2 scan (`48 AIn + 12 TIn`) with v2 24-bit ME048 packing fixes the profile when tested as a complete combination. | Patch `BiosScanSlotCount/PayloadStride/ScanStride` to 60, use `Mic140v2PackMe04848v2`/`Mic140v2PackTInMe04848v2`, add stride-60 detection, rebuild, run 3 s acceptance.     | Rejected and unsafe: no published rows, all 120-word payload rows rejected; after app stopped, two TCP probes to `192.168.14.155:4000` timed out. Treat as device hang; MIC-140 needs reboot. Active scan code was reverted to previous 48-slot state; stride-60 detection kept for diagnostics only.                                      | rejected  |

## Actions
- Created this investigation file before running acceptance.
- Built `RecorderLnx.lpi`: exit code 0.
- Ran `mic140_preview_eval.ps1 -Seconds 3 -SettleSec 0`: FAIL `pub=11 read=11 ratio=100% corrupt=0 pubGaps=0 readGaps=0 softRestart=0`, readings OK. Manual log inspection found bad published rows after block 1.
- Updated `uRecorderMic140v2Diag.pas` row-level corrupt detection to reject partial bad AIn rows before publication.
- Ran patched `mic140_preview_eval.ps1 -Seconds 3 -SettleSec 0`: FAIL `pub=1 read=1`; log showed repeated `reject all sample rows` with clean transport. The 24-channel strain plausibility hypothesis was too broad.
- Narrowed row plausibility window to first 12 channels, keeping the first-8 guard for obvious bad row heads.
- Rebuilt and ran `mic140_preview_eval.ps1 -Seconds 3 -SettleSec 0`: FAIL `pub=6 read=6`; transport still clean, but shifted/bad rows were published. Added nominal row-head guard for the current acceptance stand.
- Rebuilt and ran H7 `mic140_preview_eval.ps1 -Seconds 3 -SettleSec 0`: FAIL `pub=8 read=8 readGaps=2 publishGaps=2`. Values were cleaner, but packets `num_buff=2..4` and `8` were rejected, causing real `num_buff` gaps. Added diagnostics to log original `reject all` payloads.
- Diagnostic run: FAIL `pub=6 read=6` but no gaps. `reject raw` previews show shifted valid rows, e.g. valid AIn sequence from offset 12 in `nb=2`. Added `Mic140v2RawRowHeadNominal` and shifted-row recovery.
- H9 first run after recovery: FAIL `pub=10 read=10 readGaps=2 publishGaps=2`; log showed false-positive shifted rows with bad starts. Tightened shifted head detection to require the first 4 values in the nominal Recorder range.
- Follow-up run still published old bad row starts despite `shifted sample rows` logs. Root cause: shifted offset was found, but `Move` was skipped when recovering inside the same sample row (`lDst = lSample = 0`). Fixed copy condition to also move when `lOfs <> lSample * AStride`.
- After fixed copy, Stage A short run: FAIL `pub=11 read=11 readGaps=3 publishGaps=3`. Published `d0` values are clean; remaining rejects are regular `num_buff=3,8,13` packets where a valid first-24-AIn segment exists near the end but a full 48-word shifted row does not fit. Added partial 24-word recovery with zero-filled tail.
- After partial recovery, Stage A short run: FAIL only by count (`pub=12`, minimum `13`), with `corrupt=0`, `publishGaps=0`, `readGaps=0`, `softRestart=0`, and clean readings. Log showed first accepted block to stop interval was shorter than requested. Added a 1 s auto-preview allowance for MIC-140 CLI acceptance.
- Rebuilt after auto-preview timing allowance: exit code 0.
- Stage A acceptance: three consecutive `mic140_preview_eval.ps1 -Seconds 3 -SettleSec 0` runs passed: `pub=18/read=18`, `corrupt=0`, `publishGaps=0`, `readGaps=0`, `softRestart=0`.
- First Stage B run failed: `pub=48 read=48 ratio=100% corrupt=0 pubGaps=2 readGaps=2 softRestart=0`. Gaps came from rejected blocks `nb=13` and `nb=33`. `nb=13` raw preview contains a clean nominal first-24-AIn tail at offset 72, so shifted/partial recovery now searches absolute payload offsets and prevents overlapping source reuse.
- First wide-search patch regressed a 3 s control run: `pub=16/read=16`, `readGaps=1/publishGaps=1`. It recovered most packets as one-sample blocks and still missed `nb=13` when the nominal tail started at offset 75 with only 21 words left. Adjusted: full-row search is row-local again; partial tail search is payload-wide and may copy any nominal tail of at least 12 words, zero-filling the rest.
- Rebuilt after H12 correction: exit code 0.
- Control Stage A run after H12 correction passed: `pub=16/read=16`, `corrupt=0`, `publishGaps=0`, `readGaps=0`, `softRestart=0`.
- Stage B acceptance: three consecutive `mic140_preview_eval.ps1 -Seconds 10` runs passed: all `pub=54/read=54`, `corrupt=0`, `publishGaps=0`, `readGaps=0`, `softRestart=0`.
- First Stage C run failed count-only: `pub=159/read=159`, `corrupt=0`, `publishGaps=0`, `readGaps=0`, `softRestart=0`. Log shows a mid-run read stall after 136 blocks, several 500 ms timeouts, `stall restart 1/2`, then scan restarted at `num_buff=1`; the count deficit is from the stall window, not parser rejects.
- Repeat Stage C passed: `PASS pub=201 read=201 ratio=100% corrupt=0 pubGaps=0 readGaps=0 softRestart=0 expected=200 range=180..251 bps=5 readings=ok good=10 ch12=-7494`.
- Final Stage A on the final code passed three consecutive runs: `pub=18`, `pub=16`, `pub=19`; all had `corrupt=0`, `pubGaps=0`, `readGaps=0`, `softRestart=0`.
- User clarified that the screenshot Recorder codes are a mandatory acceptance rule and current implementation is not accepted if published codes glitch. Added text Recorder ADC profile to `Docs/devices/mic140/acceptance_tests.md`: AIn 1..24 must stay `-8200..-6300`, AIn 25..48 must stay `-24000..-13000`, and zero/saturation/positive/out-of-range published values are hard fail.
- Added `CheckPublishedRecorderCodes` in `uRecorderMic140DataSource.pas` and made `Tools/mic140_preview_eval.ps1` fail on `corruptPublish > 0` or `MIC-140 code quality violation`.
- Strengthened 3 s run failed as intended: `pub=18/read=18` with old stream counters clean but `corruptPublish=18 codeBad=18`. First violation: channel 25 in block 1 was `4078`, and raw payload showed AIn 1..24 Recorder-like while 25..48 contained saturation/garbage. This confirms H14 and shows the bug is before publication, not only in the script.
- Compared original `MIC140_48v2mod.cpp`: for dev rev 14.1 `PrepareModuleDescForScan` programs `48 AIn + 12 TIn` internal slots and packs AIn through the v2 24-bit ME048 table. Patched `uRecorderMic140v2Scan.pas` / `uRecorderMic140v2Device.pas` to use stride 60 and v2 ME048 packing.
- `48+12` v2-layout test: build OK, but run failed with no accepted stream stop; raw data was mostly out-of-profile/saturated. Device did not hang.
- Reverted to 48-word payload and tested second-half MUX descriptors:
  - `$0120` (`MUX_IN2`) failed: ch25 around positive `+18353`, `corruptPublish/codeBad > 0`.
  - `$0110` (`MUX_IN1`) improved second half to Recorder-like `-19688` values in first packet, but most later packets still had bad ch33..48; after strict full-profile filter, run was `pub=2/read=2 corruptPublish=0 codeBad=0`.
  - `$0130` (`MUX_IN1+MUX_IN2`) failed: first block zero-filled / ch25 around `+10608`, `corruptPublish/codeBad > 0`.
- Added `Mic140v2RawRowRecorderProfile` and changed `Mic140v2KeepGoodSampleRows` to accept only complete 48-channel rows matching the text Recorder code profile; removed partial zero-fill recovery from the active path.
- Tested `48+3` internal TIn stride:
  - With `MUX_IN1`: build OK, run published only 1 clean block, then rejected saturated/out-of-profile rows and did a stall restart; device stopped normally.
  - With `$0100` AIn descriptors: no published blocks, no `stream stop` line because `published=0`; device stopped normally, no reboot required.
- Tested `48 stride + v2 24-bit ME048 + MUX_IN1`: no published blocks; first packet was already out-of-profile; device stopped normally.
- Tested legacy core fallback through `uRecorderMic140v2Factory.pas`: build/run completed and device stopped normally, but strengthened 3 s run failed hard (`pub=13/read=13`, `corrupt=13`, `corruptPublish=26`, `codeBad=10`). First published block had positive/saturated garbage in ch1.., so legacy fallback is not viable. Factory reverted to v2.
- Tested complete 60-slot MIC140_48v2 layout (`48 AIn + 12 TIn`, v2 24-bit ME048 packing, stride 60). Build OK, but 3 s acceptance produced `FAIL no stream stop line`; log shows `slots=60`, `words=120`, `stride=60`, every packet `reject all sample rows`, and no published blocks. After stop, TCP probe to `192.168.14.155:4000` failed twice with timeout. This is considered a device hang; request MIC-140 reboot before continuing. Reverted active scan programming to the previous 48-slot state to avoid repeating the unsafe run.

## Acceptance Result
- Not accepted after the added Recorder code-profile rule.
- Old A/B/C PASS results are obsolete because they allowed published code glitches.
- Current best tested strict-profile run: `pub=2/read=2`, `corruptPublish=0`, `codeBad=0`; fails count acceptance.

## Current Conclusion
- Not complete yet.
- Latest live state: device appears hung after H21; TCP port `192.168.14.155:4000` does not answer. MIC-140 reboot is required before further live acceptance.
- Best current direction after reboot: continue from the reverted 48-slot strict-profile path and investigate board commutator/config loading for ch33..48. Avoid retrying legacy fallback, 60-slot v2 scan, `48+3`, `48+12`, and v2 ME048 combinations without new evidence.
