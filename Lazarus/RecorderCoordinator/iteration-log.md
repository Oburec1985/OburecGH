# Журнал итераций RecorderCoordinator

## 2026-09-14 10:15 — получение Mera Files через API

**Запрос:** перед синхронизацией получать фактический каталог Mera Files со
всех RecorderLnx через сетевой API.

**Сделано:** перед синхронизацией RCPanel очищает устаревшие пути и отправляет
всем узлам `config.get`; `mera_files_path` принимается только из результата
команды. Heartbeat оставлен для оперативного состояния. После ожидания ответов
синхронизация требует путь основного и всех получателей.

**Проверка:** RCPanel forced Win64 — exit code 0; RecorderLnx скомпилирован,
финальная линковка заблокирована работающим EXE (error 5).

**Статус:** код готов; требуется закрыть RecorderLnx и повторить его сборку.

## 2026-09-14 09:45 — запуск Samba-настройки двойным щелчком

**Запрос:** подготовить преднастроенный ярлык для запуска Samba-скрипта из
файлового менеджера.

**Сделано:** рядом со скриптом добавлен переносимый `.desktop`; путь к SH-файлу
вычисляется из расположения самого ярлыка через `%k`, права root запрашивает
`pkexec`.

**Проверка:** структура Desktop Entry проверена вручную; оба файла используют
относительное соседство и не зависят от текущего каталога.

**Статус:** готово.

## 2026-09-14 09:30 — основной Rlnx и синхронизация SDB

**Запрос:** выбрать в таблице основной RecorderLnx и копировать его каталог SDB
на остальные ПК с сохранением структуры; подготовить Samba для `Mera Files`.

**Сделано:** в таблицу добавлен признак «Основной SDB», кнопки выбора и фоновой
синхронизации. Выбор и имя SMB-ресурса сохраняются в `rcpanel.ini`. На Windows
копирование выполняет `robocopy`, на Linux — `smbclient`; ошибка одного приёмника
не мешает обработать остальные. Добавлен автономный Samba-скрипт.

**Проверка:** forced Win64-сборка RecorderCoordinator — exit code 0; Linux-сборка
и `bash -n` проходили до заключительной общей правки; повторный запуск тестовой
VM заблокирован ошибкой доступа VMware. `git diff --check` — без ошибок.

**Статус:** код готов; повторная упаковка Linux payload ожидает запуска VM.

## 2026-09-11 17:50 — удаление и фильтрация хостов

**Запрос:** добавить удаление хостов и хранить исключённые записи в отдельном
редактируемом INI-файле, чтобы автообнаружение не возвращало их в таблицу.

**Сделано:** добавлена кнопка «Удалить хост» с подтверждением. UUID, IP, имя и
MAC удалённого хоста сохраняются в `ignored-hosts.ini` рядом с `hosts.ini`;
hello, heartbeat, discovery, HostAgent и сохранённый реестр учитывают фильтр.
Версия RCPanel повышена до 0.1.12.

**Проверка:** forced-сборка RecorderCoordinator Win64 — exit code 0;
`git diff --check` — без ошибок.

**Статус:** готово.

## 2026-09-11 17:10 — изменяемая ширина колонок

**Запрос:** разрешить пользователю менять ширину колонок таблиц RCPanel.

**Сделано:** для таблиц хостов, событий и файловых хранилищ включено изменение
ширины перетаскиванием границы заголовка. Версия RCPanel повышена до 0.1.11.

**Проверка:** исходники и LFM принудительно скомпилированы без ошибок; финальная
линковка не выполнена, потому что запущенный RCPanel блокирует EXE (error 5).

**Статус:** код готов; для полной сборки нужно закрыть запущенный RCPanel.

## 2026-09-11 16:55 — повторный запрос MAC кнопкой поиска

**Запрос:** если в строке хоста MAC ещё пуст, кнопка поиска должна повторно
получать его у работающего RecorderLnx.

**Сделано:** ответ RecorderLnx на явный UDP-поиск расширен актуальным списком
локальных MAC; rcPanel принимает его вместе с UUID и IP и дополняет запись.
Версии повышены до RecorderLnx 0.1.5 и RCPanel 0.1.10.

**Проверка:** forced-сборка RecorderLnx — OK; Coordinator скомпилирован до
линковки, выходной EXE заблокирован работающим экземпляром RCPanel.

**Статус:** код готов; для полной линковки RCPanel нужно закрыть приложение.

## 2026-09-11 — Позднее слияние UUID-дубля kip4

**Запрос:** удалять автоматически восстановленную строку kip4 без сетевого
адреса, когда позже появляется строка того же ПК с адресом `192.168.9.85`.

**Сделано:** регистрация hello/discovery теперь объединяет одинаково названные
UUID-запись без адреса и адресную запись; UUID остаётся identity, IP и остальные
данные взаимно дополняются. Добавлен регрессионный сценарий для kip4.

**Проверка:** CoordinatorHostIdentityTest пересобран и выполнен — OK. Полная
компиляция исходников прошла, но линковка EXE заблокирована запущенным RCPanel
(Windows error 5).

**Статус:** исправление и тест готовы; для полной линковки нужно закрыть RCPanel.

## 2026-09-11 14:19 — Адрес редактируется у выбранной UUID-записи

**Запрос:** позволить назначить IP автоматически найденному RecorderLnx с UUID,
не создавая отдельный исчезающий alias.

**Сделано:** UI хранит identity выбранной строки отдельно от изменяемого адреса.
Пока строка выбрана, кнопка редактирует именно её; введённый IP передаётся в
модель как новый адрес, имя и конфигурация сохраняются для исходного Instance ID.

**Проверка:** forced-сборка RecorderCoordinator Win64 — OK; модельный тест
идентичности и слияния адресного alias — OK (выполнен параллельной веткой).

**Статус:** готово к интеграционной проверке сценария UUID → IP → перезапуск.

## 2026-09-11 — Один GUI-экземпляр RCPanel

**Запрос:** убрать предупреждение о занятом TCP/8765 при запуске RCPanel,
после которого оставалась внешне работающая, но лишняя форма.

**Сделано:** подтверждено, что listener стартует один раз; причиной был второй
процесс. Добавлена межпроцессная блокировка Windows/Linux до создания формы,
версия RCPanel повышена до 0.1.6. `WaitFor` не применялся.

**Проверка:** forced-сборка Win64 — OK; двойной запуск оставляет один процесс,
второй завершается до запуска HTTP API.

**Статус:** готово; Linux ELF нужно пересобрать при обновлении установщика.

## 2026-09-11 — Сшивание удалённого ручного адреса с UUID

**Запрос:** убрать дубль kip3, когда ручная запись использует IP как ID, а
RecorderLnx регистрируется с UUID, тем же IP и MAC.

**Сделано:** поиск и слияние alias теперь независимо сравнивают входящий адрес
с обоими полями существующей записи — `address` и адресным `instance_id`.
Имя не используется как identity; ручные и автоматические сведения дополняются.
Добавлен persistence-регрессионный сценарий для `192.168.9.66`.

**Проверка:** CoordinatorHostIdentityTest — OK; forced-сборка
RecorderCoordinator Win64 — OK.

**Статус:** готово.

## 2026-09-11 — HTTP-отказ launcher больше не считается принятой командой

**Запрос:** при выключении Linux-ПК RCPanel сообщал «команда принята», хотя
HostAgent мог отклонить запрос из-за настроек или прав.

**Сделано:** HTTP-клиент передаёт код ответа отдельно от транспортной ошибки.
Коды 2xx журналируются как принятая команда, остальные — как отказ launcher с
кодом и телом ответа. Любой корректный HTTP-ответ сохраняет зелёный статус
HostAgent; красным отмечается только отсутствие сетевого/HTTP-соединения.
Версия RCPanel увеличена до 0.1.5.

**Проверка:** forced-сборка RecorderCoordinator Win64 — успешно.

**Статус:** готово; фактическую причину отказа покажет следующий ответ HostAgent.

## 2026-09-11 — Отдельный статус HostAgent

**Запрос:** отображать доступность HostAgent отдельно от ping компьютера и
состояния RecorderLnx.

**Сделано:** добавлена колонка `HostAgent` со своими состояниями и цветами:
серый — не проверен, жёлтый — проверка, зелёный — доступен, красный — нет связи.
Результат команды обновляет только состояние агента.

**Проверка:** forced-сборка скомпилировала изменённые PAS/LFM; финальная
линковка заблокирована запущенным `RecorderCoordinator.exe` (error code 5).

**Статус:** код готов; после закрытия rcPanel требуется повторить сборку `-B`.

## 2026-09-11 — Тайм-аут HostAgent без first-chance ESocketError

**Запрос:** недоступный HostAgent на порту 8766 не должен останавливать
отладчик rcPanel исключением при команде запуска RecorderLnx.

**Сделано:** preflight и HTTP-команда объединены в одну небросающую сокетную
транзакцию; исключён второй connect через `TFPHTTPClient`.

**Проверка:** изменённый модуль успешно скомпилирован в составе forced-сборки;
финальная линковка заблокирована открытым отладчиком rcPanel. Подробности в
`errors/2026-09-11-host-agent-preflight-empty-http.md`.

**Статус:** код готов; для выпуска EXE закрыть остановленную debug-сессию и
повторить forced-сборку.

## 2026-09-11 — Цвет сетевой связи с ПК

**Запрос:** выделять успешную ping-связь с компьютером зелёным цветом.

**Сделано:** колонка «Связь с ПК» рисуется зелёной при доступности, красной при
ошибке, жёлтой во время проверки и серой до первой проверки. Версия повышена
до 0.1.3.

**Проверка:** forced-сборка rcPanel — OK.

**Статус:** готово.

## 2026-09-11 — Один адрес для выбора и добавления

**Запрос:** убрать дублирующее поле добавления ID/адреса и редактировать адрес
выбранного хоста в единственном поле.

**Сделано:** поле выбранного адреса стало редактируемым и управляет режимом
«Редактировать»/«Добавить хост» сразу по `OnChange`; нижний дубль удалён из LFM.
Версия rcPanel повышена до 0.1.2.

**Проверка:** forced-сборка rcPanel — OK.

**Статус:** готово.

## 2026-09-11 — Версия в заголовке и точный режим редактора

**Запрос:** показывать версию rcPanel в заголовке, повышать её при изменениях и
сразу переключать кнопку редактора для нового адреса.

**Сделано:** версия rcPanel повышена до 0.1.1 и вынесена в единый модуль;
`127.0.0.2` больше не приравнивается к существующему `127.0.0.1`, поэтому
OnChange сразу показывает «Добавить хост».

**Проверка:** модельный regression-тест и forced-сборка rcPanel — OK.

**Статус:** готово.

## 2026-09-11 — Версия rcPanel в заголовке

**Запрос:** показывать номер версии в заголовке главной формы rcPanel и
инкрементировать его при каждом изменении программы.

**Сделано:** добавлен единый модуль версии; главная форма получает из него
заголовок `RCPanel 0.1.1`. Версия увеличена с исходной пакетной 0.1.0 до 0.1.1.

**Проверка:** принудительная Win64-сборка `RecorderCoordinator.lpi` — успешно.

**Статус:** готово.

## 2026-09-11 — Редактирование имени существующего хоста

**Запрос:** при выборе строки заполнять поля ID/адреса и имени, переименовывать
существующий хост без дубля и возвращать режим добавления для нового адреса.

**Сделано:** редактор синхронизирован с выбором строки; кнопка автоматически
переключается между «Редактировать» и «Добавить хост». Имя сохраняется в модели,
постоянном реестре и legacy-конфигурации; обновление таблицы не затирает ввод.

**Проверка:** forced-сборка RecorderCoordinator и существующий
CoordinatorHostIdentityTest — OK.

**Статус:** готово.

## 2026-09-11 — Отдельный статус сетевой связи с ПК

**Запрос:** показывать ход и результат ping отдельно от состояния RecorderLnx
и HostAgent, не блокируя форму.

**Сделано:** модель публикует `unknown/checking/reachable/unreachable`; фоновый
host monitor выставляет `checking` перед асинхронным ping. В таблицу формы
добавлена отдельная LFM-колонка «Связь с ПК».

**Проверка:** forced-сборка RecorderCoordinator — OK; модельный тест всех
четырёх переходов CoordinatorHostIdentityTest — OK.

**Статус:** готово.

## 2026-09-11 — Сшивание ручного и автоматического хоста

**Запрос:** показывать вручную добавленный локальный хост и автоматически
зарегистрированный RecorderLnx одной строкой, взаимно дополняя сведения.

**Сделано:** UUID Recorder стал каноническим ID; объединяются все совпадения
по нормализованному адресу и MAC, включая IPv4-mapped loopback. При слиянии
сохраняются ручная подпись, адрес, MAC, путь EXE, ОС и актуальный статус;
пустые сохранённые значения не затирают заполненные.

**Проверка:** `CoordinatorHostIdentityTest` и forced-сборка
`RecorderCoordinator.lpi` — OK.

**Статус:** готово.

## 2026-09-11 — Тест сшивания ручного и автоматического хоста

**Запрос:** гарантировать одну запись для вручную добавленного локального хоста
и автоматически зарегистрированного RecorderLnx, сохранив данные обоих.

**Сделано:** модельный тест проверяет слияние `127.0.0.1`/`localhost` и
`::ffff:127.0.0.1` с UUID, взаимное дополнение имени, адреса, MAC, ОС и пути,
а также схлопывание раздельных address/MAC aliases; разные удалённые адреса
остаются разными даже при одинаковом отображаемом имени.

**Проверка:** forced-компиляция и запуск CoordinatorHostIdentityTest — OK.

**Статус:** тестовый контракт готов; production-код не менялся.

## 2026-09-11 10:31 — команды управления ПК из формы

**Запрос:** вывести в rcPanel адресные и групповые команды запуска RecorderLnx,
Wake-on-LAN и выключения ПК, не блокируя интерфейс и не затрагивая запись.

**Сделано:** в редактируемую LFM добавлены шесть кнопок; запуск и выключение
отправляются существующим фоновым HTTP-клиентом, WOL использует MAC из модели.
Кнопки записи, Preview и Stop сохранены без изменения поведения.

**Проверка:** forced-сборка RecorderCoordinator Win64 — OK.

**Статус:** готово.

## 2026-09-11 — Приём inventory от RecorderLnx

**Запрос:** получать MAC, путь исполняемого файла, ОС и hostname через уже
существующее соединение RecorderLnx с rcPanel.

**Сделано:** модель принимает, возвращает и сохраняет inventory в реестре
хостов; сшивание хостов сохраняет эти сведения. Добавлен модельный тест.

**Проверка:** forced-сборка Coordinator и CoordinatorHostIdentityTest — OK.

**Статус:** готово.

## 2026-09-11 10:23 — Общий переход в просмотр

**Запрос:** добавить видимую общую кнопку перехода всех RecorderLnx в режим просмотра между общими кнопками остановки и записи.

**Сделано:** в редактируемую LFM-форму добавлена `TBitBtn` с жёлтой иконкой №2; обработчик отправляет всем хостам команду `recording.preview`. Кнопки размещены в порядке Stop → Preview → Record → Search.

**Проверка:** принудительная Win64-сборка RecorderCoordinator завершилась успешно.

**Статус:** готово.

## 2026-09-11 10:10 — UI и полный контур управления хостами

**Запрос:** исправить форму rcPanel и параллельно реализовать сохранение хостов,
ping ПК, запуск RecorderLnx, выключение и Wake-on-LAN через отдельный агент.

**Сделано:** командные кнопки переведены в LFM-совместимые `TBitBtn` с
ImageList; лупа поиска назначена индексом 33 и уменьшена до 18 px. Ручное
управление listener убрано, «Поиск» запускает немедленный UDP
discovery и опрос HostAgent. Таблица раздельно показывает ПК, агент и Recorder.
Добавлены групповые и адресные команды запуска процесса, WOL и выключения.

**Проверка:** forced-сборки RecorderCoordinator, RecorderLnx и RecorderHostAgent
Win64 — OK; HostIdentityTest и локальный HTTP status HostAgent — OK.

**Статус:** Windows-контур готов; Linux ELF агента и включение его в общий
offline installer остаются отдельным шагом из-за заблокированной Astra VM.

## 2026-09-11 — Реестр хостов и раздельная доступность

**Запрос:** сохранять автоматически найденные хосты и раздельно учитывать
доступность ПК, HostAgent и состояние RecorderLnx без блокировки GUI.

**Сделано:** добавлены постоянный INI-реестр identity/address/name/MAC,
слияние по ID/адресу/MAC, модельный API поиска и статусов, фоновый ICMP-monitor.
Recorder heartbeat, HostAgent status и ping ПК представлены разными полями;
в конфигурацию добавлены host_agent port/token без вывода токена в JSON.

**Проверка:** Windows-сборка RecorderCoordinator OK; существующий
CoordinatorHostIdentityTest — OK. Старый HTTP lifecycle test по-прежнему
нарушает собственный предел Stop <20 мс и требует отдельной диагностики.

**Статус:** слой Core/Service готов; UI-интеграция выполняется отдельно.

## 2026-09-11 — Аудит управления компьютерами

**Запрос:** определить, какие из ранее задуманных функций управления хостами
уже существуют: сохранение, ping, выключение ПК, запуск RecorderLnx и WOL.

**Сделано:** подтверждены ручное сохранение id/name, UDP discovery,
hello/heartbeat, получение состояния/пути и команды записи работающему Recorder.
Автосохранения найденных хостов, ping ОС, shutdown, запуска процесса и WOL нет.

**Проверка:** read-only аудит Config/Model/HTTP/UI, клиентского dispatcher и LFM;
результаты независимо сверены тремя агентами.

**Статус:** исследование готово; требуется отдельная реализация управления ПК.

## 2026-09-09 — Рабочий desktop-ярлык rcPanel как в DEB RecorderLnx

**Запрос:** повторно сравнить неработающий ярлык rcPanel с рабочим ярлыком
RecorderLnx из DEB и устранить фактическое различие.

**Сделано:** shell-инсталлятор теперь определяет XDG Desktop, копирует туда
`rcPanel.desktop`, назначает владельца и mode 0755. Пользовательский ярлык
использует совместимый с FLY абсолютный PNG из pixmaps.

**Проверка:** реальная установка в Atra VM; `desktop-file-validate` — OK;
`gio launch` пользовательского ярлыка успешно запустил RecorderCoordinator.

**Статус:** готово.

## 2026-09-09 23:50 — финальная Linux-сборка rcPanel

**Запрос:** после исправлений managed HTTP, SQL config path и
нормализации installer повторно собрать и упаковать Linux rcPanel.

**Сделано:** forced-сборка `RecorderCoordinator.lpi` в Atra VM;
`prepare-installer.ps1` обновил ELF и checksum в `firebird/linux/payload`.

**Проверка:** build — OK; ELF magic `7F454C46`; source/payload/checksum SHA-256
`6883ef71908cf0598535810e4dc0832d2c99c735da8e9bf5d49a43867da4638e`;
`bash -n` install/check — OK; ConfigPath, HostIdentity, EventGrouping и HTTP lifecycle — passed.

**Статус:** готово.

## 2026-09-09 23:40 — самовосстановление SQL-пути rcPanel

**Запрос:** исправить отключение SQL event store из-за многократно испорченного
абсолютного Linux-пути и восстанавливать уже сохранённые конфигурации.

**Сделано:** Unix-пути распознаются независимо от платформенной семантики drive;
при старте повреждённый либо отсутствующий путь заменяется существующим штатным
кандидатом и каноническое значение сохраняется обратно в `coordinator.ini`.

**Проверка:** `CoordinatorConfigPathTest` — passed; forced Win64-сборка
RecorderCoordinator — OK.

**Статус:** готово; Linux-сборку выполняет корневая задача.

## 2026-09-09 23:25 — канонический SQL-путь Linux rcPanel

**Запрос:** устранить накопление префикса `/opt/mera/RecorderCoordinator` в
`events.sql_db_config` после повторных запусков и обновлений rcPanel.

**Сделано:** Linux-инсталлятор теперь при новой установке и upgrade записывает
канонический абсолютный путь `/var/opt/mera/RecorderLnx/config/projects/default/sql-db.ini`,
не меняя флаги создания событий и каскадного старта; проверочный скрипт
контролирует deployed-путь. В новом INI явно записывается настройка каскадного старта.

**Проверка:** статический аудит installer/launcher; локальный `bash` отсутствует,
а VM Atra выключена, поэтому `bash -n` требуется выполнить при следующей сборке payload.

**Статус:** готово в исходниках установщика.

## 2026-09-09 23:20 — HTTP-клиенты становятся управляемыми

**Запрос:** исправить каскадный старт, когда реально подключённые по HTTP
RecorderLnx оставались `managed=False`, а ручная запись не запускала соседей.

**Сделано:** успешный HTTP hello/heartbeat теперь помечает RecorderLnx как
управляемый независимо от отсутствующего клиентского поля `managed`; UDP
discovery по-прежнему не даёт права на команды. Тест покрывает переход
discovery → HTTP и постановку `recording.start` соседнему клиенту.

**Проверка:** `CoordinatorHostIdentityTest` пересобран и выполнен — OK.

**Статус:** готово к общей сборке Windows/Linux и стендовой проверке.

## 2026-09-09 19:54 — обновлён Linux rcPanel в офлайн-установщике

**Запрос:** собрать для Linux актуальный rcPanel с последними
исправлениями диагностики, каскадной записи и SQL-событий.

**Сделано:** forced-сборка `RecorderCoordinator.lpi` в Atra VM;
ELF скопирован в `firebird/linux/payload` штатным
`prepare-installer.ps1`, пересоздан `RecorderCoordinator.sha256`.

**Проверка:** Linux-сборка — OK; ELF magic `7F454C46`; source/payload/checksum
совпадают, SHA-256 `580a1a0f4d40c536e21eb444ec6db58a9d916e085ac092e25272b8521b1ca9d8`;
`bash -n` для install/check — OK; `git diff --check` — OK.

**Статус:** готово.

## 2026-09-09 — Значок Linux-ярлыка rcPanel

**Запрос:** исправить красный стандартный значок вместо иконки Coordinator в
Linux-инсталляторе.

**Сделано:** переносимый desktop-файл получил валидный `Exec` и системную
иконку установщика. Установленный rcPanel использует `Icon=rcpanel`; PNG
устанавливается в hicolor и `/usr/share/pixmaps`, проверка контролирует оба пути.

**Проверка:** `desktop-file-validate` и `bash -n` обоих скриптов в Atra VM — OK;
BAT подготовки переносимого комплекта — OK.

**Статус:** готово.

## 2026-09-09 — Firebird lifecycle и payload массовой SQL-настройки

**Запрос:** перепроверить события на schema v5 и сохранность payload команды
`config.sql_database.set` при массовой постановке через `*`.

**Сделано:** Firebird-тест приведён к schema v5 (`display_name`) и очищает
оставшиеся тестовые события; host-тест проверяет wildcard Queue → NextCommand
с сохранением `host` и `port`.

**Проверка:** CoordinatorFirebirdLifecycleTest на project `sql-db.ini` — passed;
CoordinatorHostIdentityTest — OK.

**Статус:** готово.

## 2026-09-09 — Автозапуск по фактическому переходу в запись

**Запрос:** исправить случай, когда rcPanel видит `recording`, но не запускает
остальные управляемые RecorderLnx и не даёт достаточной диагностики событий.

**Сделано:** edge `не recording -> recording` теперь запускает соседей без
ожидания `recording_id`; lifecycle MERA остаётся отдельным сообщением.

**Проверка:** CoordinatorHostIdentityTest без `recording_id` — OK;
forced build RecorderCoordinator и RecorderLnx — OK.

**Статус:** готово; требуется контрольный прогон на двух ПК.

## 2026-09-09 — Массовая настройка Firebird и иконка rcPanel

**Запрос:** добавить в настройках хостов установку одного Firebird-сервера всем
RecorderLnx и исправить значок установленного rcPanel.

**Сделано:** добавлены LAN IPv4 по умолчанию, кнопка массовой команды и передача
JSON payload; RecorderLnx сохраняет новый `Host` в `sql-db.ini` и перезапускает
SQL runtime через штатный менеджер. Узкая команда разрешена управляемым клиентам,
не открывая произвольное удалённое редактирование. Linux desktop entry использует
абсолютный путь к поставляемому `payload/rcpanel.png`.

**Проверка:** forced-сборки RecorderCoordinator и RecorderLnx — OK;
`git diff --check` — OK. `bash -n` недоступен в текущей Windows-среде.

**Статус:** готово.

## 2026-09-09 — Стабильные адрес и состояние найденного RecorderLnx

**Запрос:** убрать мигание локального хоста между localhost/IP и
discovered/runtime-состоянием; проверить связь дефекта с автозапуском остальных.

**Сделано:** UDP discovery отделён от подтверждённой HTTP-регистрации. Свежие
hello/heartbeat теперь имеют приоритет и не перетираются повторными broadcast;
после истечения HTTP-liveness discovery снова может показать найденный endpoint.

**Проверка:** forced-сборка RecorderCoordinator — OK; identity-тест проверяет
сохранение HTTP-адреса/recording после UDP-анонса и постановку peer start — OK.

**Статус:** готово.

## 2026-09-09 — BAT подготовки переносимого Linux-инсталлятора

**Запрос:** сделать в каталоге Firebird/rcPanel BAT, после которого папку можно
сразу копировать на флешку и устанавливать на другом компьютере.

**Сделано:** `update-offline-installer.bat` обновляет rcPanel payload и SHA-256,
проверяет Firebird-архив, установочные скрипты, ярлык, документацию и payload.
Необязательные `deps/*.deb` сохраняются и только диагностируются.

**Проверка:** BAT выполнен с `--no-pause`, полный комплект признан готовым.

**Статус:** готово.

## 2026-09-09 — UTF-8 в журнале rcPanel под Linux

**Запрос:** убрать кракозябры в строках журнала rcPanel на Linux.

**Сделано:** активные UTF-8 модули Coordinator с русскими литералами явно
помечены `{$codepage UTF8}`. Устранено смешение обычного литерала с
`TJSONData.AsJSON: UTF8String`; сетевой протокол не менялся.

**Проверка:** forced-сборки Windows и Linux — OK; Linux payload обновлён.

**Статус:** готово.

## 2026-09-09 — План проверки UDP discovery

**Запрос:** подготовить независимую проверку взаимного broadcast-обнаружения
RecorderLnx и rcPanel на одном ПК и в локальной сети.

**Сделано:** добавлены диагностический UDP responder/discover и сценарий для
loopback, directed broadcast, нескольких NIC, firewall и конфликта порта.
Production-файлы не изменялись.

**Проверка:** два локальных процесса обменялись запросом и JSON-ответом;
`RESULT passed replies=1`.

**Статус:** тестовая оснастка готова; LAN-прогон требует второго ПК.

## 2026-09-09 — Иконка rcPanel и ярлык Linux

**Запрос:** сделать собственную иконку rcPanel, встроить её в программу и
установщик, пересобрать Windows и Linux варианты.

**Сделано:** создан и подключён Windows MAINICON; Linux PNG включён в payload
инсталлятора и назначен desktop-ярлыку `rcpanel`. Manifest SHA-256 теперь
проверяет как ELF, так и значок.

**Проверка:** Windows и Linux forced-сборки — OK; извлечённая иконка Windows EXE
визуально проверена; payload обновлён.

**Статус:** готово.

## 2026-09-09 — Галочка автозапуска остальных RecorderLnx

**Запрос:** добавить в Coordinator настройку, при которой старт записи одного
добавленного RecorderLnx запускает запись на остальных добавленных хостах.

**Сделано:** на панели команд хостов добавлена галочка с немедленным сохранением;
в конфигурацию добавлен выключенный по умолчанию ключ
`[commands] start_all_on_any_recording`. Модель отслеживает только фактический
переход heartbeat в `recording`, ставит `recording.start` остальным хостам и
исключает инициатора, уже записывающих и уже имеющих pending/accepted start.
CLI загружает настройку и сохранённый список хостов в модель.

**Проверка:** просмотрен wiring PAS/LFM, `git diff --check` — OK; выполнена
forced-сборка `RecorderCoordinator.lpi` — OK.

**Статус:** готово. GUI передаёт значение при старте, сохранении настроек и
непосредственно при переключении галочки. В группу входят только сохранённые и
доступные хосты; автозарегистрированный посторонний клиент её не запускает.

## 2026-09-09 — Действия с событием из списка Coordinator

**Запрос:** добавить для выбранной строки события отдельные действия открытия
MERA-замеров и безопасного удаления события.

**Сделано:** добавлены кнопки «Открыть замеры» и «Удалить событие»; открытие
переиспользует общий диалог пакетов, удаление вызывает SQL-репозиторий после
подтверждения и затем перечитывает список. Физические MERA-файлы не удаляются.

**Проверка:** просмотрены PAS/LFM и выполнен `git diff --check`; сборка не
запускалась по условию текущей подзадачи.

**Статус:** правки готовы к общей сборке Coordinator.

## 2026-09-09 15:46 — Потокобезопасный диагностический лог Coordinator

**Запрос:** писать рядом с EXE подробный лог, чтобы проследить HTTP-запрос,
группировку события и сохранение lifecycle в SQL без обращения к GUI из worker.

**Сделано:** подключён `uSharedFileLogger` с файлом `RecorderCoordinator.log`;
добавлены receive/result HTTP, event/recording IDs группировки и begin/success/error
SQL с путём. GUI `AddLog` дублирует сообщения в тот же файл; пароли не пишутся.

**Проверка:** forced-сборка `RecorderCoordinator.lpi` — OK; файл рядом с EXE
создан, старт процесса записан с timestamp и thread ID.

**Статус:** готово.

## 2026-09-09 — SQL-события за выбранный интервал

**Запрос:** показывать в Coordinator события SQL за заданный период и открывать
выбранное событие в общем диалоге для просмотра и редактирования.

**Сделано:** добавлены поля начала и окончания, явная кнопка обновления,
SQL-таблица событий и кнопка редактирования. Частый таймер хостов больше не
читает SQL; после закрытия общего диалога список перечитывается.

**Проверка:** forced-сборка RecorderCoordinator — OK после согласования с
расширенным общим диалогом событий.

**Статус:** готово.

## 2026-09-09 — Карточка подключения SQL-базы

**Запрос:** при выборе Firebird в таблице показывать каталог базы и остальные
параметры подключения.

**Сделано:** SQL-строка заполняет read-only поля: имя, backend, полный путь
или серверный alias базы, `host:port` и пользователя. В подсказке отображается
файл SQL-конфигурации; файловые хранилища продолжают редактироваться отдельно.

**Проверка:** forced-сборка RecorderCoordinator — OK; свежий EXE запущен.

**Статус:** готово.

## 2026-09-09 — SQL-сервер и файловый архив в одной таблице

**Запрос:** показывать во вкладке файловых хранилищ SQL-хост и сетевой каталог,
а также проверить фактическую запись событий замеров в SQL.

**Сделано:** первой информационной строкой выводятся backend, база и SQL
`host:port`; ниже отображаются архивы с каталогом и файловым сервером. SQL-строка
не редактируется как файловое хранилище. Подтверждена граница SQL persistence.

**Проверка:** forced-сборка RecorderCoordinator — OK; свежий EXE запущен.

**Статус:** UI готов; перенос MERA в сетевой архив ещё не реализован.

## 2026-09-09 — Убрана таймерная задержка статуса RecorderLnx

**Запрос:** передавать и показывать Stop/Preview/Record сразу после события,
а не ждать периодических таймеров.

**Сделано:** GUI Coordinator обновляет модель каждые 250 мс вместо 2000 мс.
RecorderLnx при изменении snapshot отдельно будит сетевой worker и немедленно
отправляет heartbeat; период 3000 мс сохранён только для контроля доступности.

**Проверка:** forced-сборки RecorderCoordinator и RecorderLnx — OK.

**Статус:** готово.

## 2026-09-09 — Перерисовка цветового статуса

**Запрос:** при состоянии `preview` цветовой индикатор оставался чёрным.

**Сделано:** после обновления состояния хоста принудительно перерисовывается
ячейка индикатора. Соответствие цветов не менялось: preview — жёлтый, запись —
зелёный, остановка — серый, недоступность — чёрный.

**Проверка:** forced-сборка RecorderCoordinator — OK.

**Статус:** готово.

## 2026-09-09 — Синхронное обновление индикатора состояния

**Запрос:** убрать задержку colorbox относительно уже обновившегося текста
`preview` в таблице хостов.

**Сделано:** состояние записывается до отрисовки индикатора; статусная ячейка
явно инвалидируется и сразу обновляется. Для прямоугольника задана сплошная
кисть, чтобы стандартная отрисовка LCL не оставляла прежнюю заливку.

**Проверка:** forced-сборка RecorderCoordinator — OK; свежий EXE запущен.

**Статус:** готово.

## 2026-09-09 — Единый хост, runtime-статусы и панель управления

**Запрос:** объединять ручной локальный адрес с авто-регистрацией RecorderLnx,
показывать Stop/Preview/Record цветом и разделить групповые и адресные команды.

**Сделано:** aliases локального адреса сшиваются с устойчивым UUID; heartbeat
передаёт `stopped/preview/recording`, а устаревший хост становится `offline`.
В LFM добавлены отдельный цветной квадрат, адрес и состояние, групповые команды
сверху и `Стоп/Просмотр/Запись` для выбранного адреса снизу.

**Проверка:** identity/state test — OK; forced-сборки Coordinator и RecorderLnx
— OK. В реальном запуске ручная и автоматическая записи сшились в одну строку;
удалённая команда дала переход `stopped -> preview -> stopped`, оба GUI отвечали.

**Статус:** готово.

## 2026-09-09 — Состояния heartbeat RecorderLnx

**Запрос:** показывать фактический режим RecorderLnx вместо постоянного `ready`.

**Сделано:** повторная регистрация heartbeat больше не сбрасывает состояние;
сохранены состояния `stopped`, `preview`, `recording`, а просроченный heartbeat
выдаётся как `offline`. Добавлен модельный регрессионный тест переходов.

**Проверка:** forced-сборка Coordinator — OK; CoordinatorHostIdentityTest — OK.

**Статус:** готово; отдельный блокер end-to-end — `--serve` не слушает порт.

## 2026-09-09 — Наглядное управление RecorderLnx-хостами

**Запрос:** разделить групповые и адресные команды, показать
цветное состояние и убрать визуальное дублирование полей.

**Сделано:** сверху добавлены команды для всех; снизу — ID выбранного
хоста и `Стоп / Просмотр / Запись`. Ручное добавление оставлено
отдельной строкой. Состояние отмечается чёрным, серым, жёлтым или
зелёным индикатором.

**Проверка:** forced-сборка `RecorderCoordinator.lpi` — OK;
`git diff --check` UI/LFM — без ошибок.

**Статус:** UI готов; команда `recording.preview` должна быть поддержана
диспетчером RecorderLnx.

## 2026-09-09 — Остановка listener без фиктивного HTTP-клиента

**Запрос:** устранить `EHTTP: No REQUEST_METHOD passed from server`, которое
проявилось при запуске RecorderLnx рядом с Coordinator.

**Сделано:** серверная остановка больше не создаёт пустое TCP wake-соединение.
Она сбрасывает accepting и завершается через ограниченный select timeout;
`OnAcceptIdle` и `WaitFor` не используются. Клиент RecorderLnx выполняет
полноценный HTTP preflight вместо `connect/close`.

**Проверка:** 100 циклов реального bind/start/stop без клиентов прошли без
EHTTP; Stop не блокирует GUI, listener завершается примерно за 62 мс.
В сквозном запуске Coordinator → RecorderLnx оба процесса отвечали, сервер
принял регистрацию и heartbeat клиента (`state=ready`). Forced-сборки обоих
проектов — OK; тестовые процессы остановлены.

**Статус:** готово.

## 2026-09-09 — UI возвращён к явному service-контракту

**Запрос:** убрать из GUI широкий `Exception` catch, который мог
маскировать ошибки программирования.

**Сделано:** удалены catch-обёртки `Start`, `Stop`, `Active/Stopping`;
UI снова получает ожидаемые ошибки через `LastError`. Async-кнопка,
`Stopping/Active` polling и `fStopLogPending` сохранены.

**Проверка:** forced-сборка `RecorderCoordinator.lpi` — OK;
`git diff --check` UI — без ошибок.

**Статус:** готово.

## 2026-09-09 — Исключения HTTP lifecycle не выходят из GUI

**Запрос:** проверить async-stop и не допустить необработанного `EHTTP`
в обработчиках LCL.

**Сделано:** UI-граница перехватывает и превращает в журнал/диалог
исключения `Start`, `Stop` и опроса `Active/Stopping`; повторяющаяся
ошибка timer-опроса логируется один раз.

**Проверка:** forced-сборка `RecorderCoordinator.lpi` — OK;
`git diff --check` для UI — без ошибок.

**Статус:** UI готов; паузу отладчика на break-on-raise может исключить
только перехват внутри server-слоя.

## 2026-09-08 — Неблокирующая остановка в GUI

**Запрос:** устранить зависание формы при остановке HTTP-сервиса без
запущенного RecorderLnx.

**Сделано:** кнопка сразу переходит в отключённое состояние
`Останавливается...`; таймер опрашивает `Stopping/Active`, возвращает
кнопку и пишет в журнал только после фактической остановки.

**Проверка:** forced-сборка `RecorderCoordinator.lpi` — OK;
`git diff --check` для UI-файлов — без ошибок.

**Статус:** готово.

## 2026-09-08  — Занятый TCP-порт не завершает GUI

**Запрос:** проверить повторный запуск Coordinator при занятом порте 8765 и
исключить выход исключения из серверного слоя.

**Сделано:** подтверждено, что `TCoordinatorHttpServer.Start` без исключения
записывает причину в `LastError`; правка серверного слоя не потребовалась.

**Проверка:** forced-сборка Windows — OK; при занятом локальном 8765 второй GUI
оставался запущен и доступен для показа сообщения об ошибке.

**Статус:** готово.

## 2026-09-04 20:50 — Первый рабочий Coordinator

**Запрос:** создать включаемую в Lazarus-группу утилиту управления RecorderLnx
с GUI/CLI, сетевым API, моделью хостов/событий и настройкой хранилищ.

**Сделано:** создан отдельный проект с редактируемой LFM-формой, HTTP JSON API,
heartbeat и путём текущего замера, очередью команд и групповым start с общим
correlation ID. Добавлены local/SMB/SFTP-настройки; local/SMB проверяются
созданием и удалением тестового файла.

**Проверка:** forced-сборка Windows — OK; сервис запущен на localhost, проверены
status, hello, heartbeat, current recording и выдача двух команд группового start.

**Статус:** базовый Coordinator готов. Фактическая синхронизация файлов и SFTP
транспорт ещё не реализованы; они требуют отдельного адаптера и очереди копирования.
# Итерации RecorderCoordinator

## 2026-09-09 23:58 — Стабилизирован Firebird lifecycle-тест

**Запрос:** устранить повторный провал проверки видимости события через
production interval query.

**Сделано:** тестовый lifecycle envelope приведён к реальному контракту
Recorder: добавлен `sent_at_utc`; проверяется, что после коррекции часов
`finished_at_utc >= started_at_utc`.

**Проверка:** тест пересобран и пройден на project Firebird; production-запрос
видит событие, фактический attachment выведен в результат.

**Статус:** готово.

## 2026-09-09 23:55 — Идентичность SQL БД в журнале

**Запрос:** отличить успешную запись события от чтения другой БД и учесть
скрытие события временным диапазоном SQL-тренда.

**Сделано:** `SQL event store OK` содержит backend, server, database,
фактический Firebird attachment и путь конфига. Lifecycle-тест проверяет
видимость созданного события через production interval query.

**Проверка:** RecorderCoordinator и RecorderLnx Win64 собраны с `-B`.

**Статус:** готово.

## 2026-09-09 23:40 — Проверяемая запись и немедленное отображение события

**Запрос:** выяснить, почему при успешном lifecycle событие не видно в БД и
SQL-тренде, и сделать диагностику однозначной.

**Сделано:** после каждого SQL lifecycle выполняется read-after-write по
`event_id`; журнал показывает UTC, число замеров и точный backend/server/database.
Открытая вкладка событий автоматически перечитывается после подтверждения.
Исправлена застывшая со времени запуска верхняя граница «До»: в режиме по
умолчанию она следует за текущим временем, ручной исторический интервал сохранён.

**Проверка:** CoordinatorFirebirdLifecycleTest с новым fresh-read assertion —
passed; forced-сборка RecorderCoordinator Win64 — OK; `git diff --check` — OK.

**Статус:** готово для Linux-сборки и стендовой проверки.

## 2026-09-11 — Общий переход в просмотр

**Запрос:** добавить между общими командами остановки и записи кнопку перехода
в просмотр для всех RecorderLnx.

**Сделано:** в верхнюю панель добавлена кнопка с жёлтой иконкой просмотра;
она отправляет всем хостам команду `recording.preview`. Порядок кнопок:
стоп, просмотр, запись, поиск.

**Проверка:** forced-сборка RecorderCoordinator Win64.

**Статус:** готово.
## 2026-09-11 — GTK-выравнивание и падение на ICO

**Запрос:** выровнять общие иконки rcPanel на Linux и устранить Windows
`FPImageException` при запуске.

**Сделано:** четыре общие команды переведены на одинаковые `TSpeedButton`
40x32 с изображением 24 px. Генератор `rcPanel.ico` переведён с PNG-кадров на
совместимые с LCL несжатые 32-bit DIB-кадры.

**Проверка:** все семь ICO-кадров имеют DIB header; forced-сборка Win64 — OK;
свежий EXE запущен и контрольное время оставался активен.

**Статус:** готово; визуальное выравнивание GTK проверить новой Linux-сборкой.

## 2026-09-11 — Недоступный RecorderHostAgent без исключения отладчика

**Запрос:** при команде запуска RecorderLnx недоступный launcher не должен
останавливать отладчик Lazarus исключением `ESocketError`.

**Сделано:** перед `TFPHTTPClient` добавлена кроссплатформенная небросающая
TCP-проверка через nonblocking socket, `select` и `SO_ERROR`. Если порт агента
недоступен, задача возвращает текст ошибки через штатный callback и не вызывает
HTTP-клиент. `RequestBody` теперь всегда отсоединяется в `finally` до
освобождения потока запроса.

**Проверка:** forced-сборка RecorderCoordinator Win64 — OK; `git diff --check`
для изменённого сетевого модуля — OK.

**Статус:** готово.
## 2026-09-11 — Стабильный UUID и редактируемый IP kip4

**Запрос:** не подменять адрес kip4 UUID-кодом и сохранять вручную заданный IP после перезапуска RCPanel.

**Сделано:** UUID и сетевой адрес разделены; выбранная UUID-запись редактируется по стабильной identity, IP сохраняется через `SetHostAddress`; старый IP-alias удаляется также из legacy-конфигурации. RCPanel повышен до 0.1.7. Для старого Linux FPC добавлены именованные POSIX-константы файловой блокировки.

**Проверка:** forced-сборка RecorderCoordinator Win64 — OK; `CoordinatorHostIdentityTest` с циклом сохранение → перезапуск → загрузка — OK. Linux-сборку нужно повторить в пользовательской Linux-среде.

**Статус:** готово в исходниках.
# 2026-09-11 16:00 — MAC и диагностика Wake-on-LAN

**Запрос:** явно показывать, получил ли rcPanel MAC хоста, и объяснять отказ
Wake-on-LAN вместо молчаливого безрезультатного нажатия.

**Сделано:** в таблицу хостов добавлена колонка MAC; перед WOL MAC проверяется,
при его отсутствии/ошибочном формате показывается предупреждение и запись в
журнал. Успешная отправка журналирует MAC и адрес broadcast; версия — 0.1.8.

**Проверка:** Pascal и LFM скомпилированы до стадии линковки без ошибок; финальная
линковка заблокирована запущенным `RecorderCoordinator.exe` (PID 12436).

**Статус:** частично — для подтверждения сборки нужно закрыть запущенный rcPanel
и повторить `lazbuild -B`.
# 2026-09-11 17:10 — последний путь замера сохраняется после stop

**Запрос:** при остановленном RecorderLnx пустой `local_path` heartbeat не должен
очищать последний известный каталог замера в rcPanel.

**Сделано:** модель обновляет путь только непустым значением; добавлен регрессионный
сценарий `recording` с путём → `stopped` с пустым путём.

**Проверка:** `CoordinatorHostIdentityTest` пересобран и выполнен — OK.

**Статус:** готово.
## 2026-09-11 — Рассылка единой конфигурации Firebird

**Запрос:** исключить разные адреса и пароли при подключении RecorderLnx к
одной БД.

**Сделано:** кнопка настройки БД для всех хостов загружает host/port/database/
username/password из единственного `sql-db.ini`, указанного в конфигурации
RCPanel, и рассылает этот набор целиком. При пустом пароле команда не уходит и
показывается точный файл, который нужно настроить. Пароль в исходниках не
дублируется. Версия RCPanel увеличена до 0.1.13.

**Проверка:** forced Win64-сборка RecorderCoordinator — exit code 0.

## 2026-09-11 — Единый пароль Firebird 123

**Запрос:** одинаковая авторизация всех RecorderLnx без настройки пароля на
каждом хосте.

**Сделано:** общий SQL-конфиг и команда рассылки используют централизованный
пароль Firebird `123`; версия RCPanel повышена до 0.1.14.
