# Стабилизация и изоляция зума/панорамирования осей в OGlChart

### Выполненные задачи:
1. **Программный выбор оси при старте**:
   - Нашли правильное имя оси `PerfAxis1DAxisY` (префикс `PerfAxis1D` + `AxisY`).
   - Добавили рекурсивный поиск объекта по имени в `unit1.pas` (метод `FindNodeByName`) и автоматический выбор `PerfAxis1DAxisY` при старте.
   - Обеспечили обновление статус-бара `UpdateStatusBar` сразу при создании формы.
   
2. **Изоляция зума и панорамирования**:
   - Обнаружили, что файлы слушателей (`uOglChartPanZoomListener.pas` и др.) имеют переводы строк `LF` вместо `CRLF`, из-за чего патчи не применялись.
   - Написали и применили скрипт `apply_pan_zoom_patch.py`, нормализующий окончания строк к `CRLF` и внедряющий проверку `lRenderer.SelectedObject` для ограничения зума/панорамирования только на выбранную ось.
   
3. **Автоматическое тестирование (Панорамирование)**:
   - Создали и запустили `test_pan_interactive.py`, имитирующий правый клик и перетаскивание мыши.
   - Логи подтвердили успешную изоляцию:
     - Лимиты выбранной оси `PerfAxis1DAxisY` изменились (`MinValue=-1.5306`).
     - Лимиты невыбранной оси `PerfAxis2DAxisY` остались неизменными (`MinValue=0.0000`).

Тест пройден успешно!
