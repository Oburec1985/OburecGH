# План доработок OGlChart

- [x] Шаг 1: Изменение `uOglChartAxis.pas` — удаление `Orientation`/`TChartAxisOrientation`, добавление признака `UseOwnX` и собственных настроек оси X (`XMinValue`, `XMaxValue`, `XScale`).
- [x] Шаг 2: Изменение `uOglChartPage.pas` — добавление настроек оси X по умолчанию (`XMinValue`, `XMaxValue`, `XScale`).
- [x] Шаг 3: Изменение `uOglChartTrend.pas` — удаление ссылки на `XAxis` из `cBaseTrend`, так как координаты X теперь масштабируются по оси X страницы или собственной оси Y-оси.
- [x] Шаг 4: Изменение `uOglChartRenderer.pas` — обновление проекции координат X по новой логике, поддержка редактирования меток диапазона X (`celXMin`, `celXMax`), рисование всех осей Y.
- [x] Шаг 5: Обновление тестового примера `Test_component/unit1.pas` для создания графиков с индивидуальными осями Y.
- [x] Шаг 6: Разработка системы фрейм-листенеров в новом модуле `uOglChartFrameListener.pas` (базовый класс `TChartFrameListener`, слушатель выбора `TChartSelectListener`, слушатель панорамирования/масштабирования `TChartPanZoomListener`).
- [x] Шаг 7: Реализация подсветки выделенных и наведенных объектов (страницы, оси) во время отрисовки в `TOpenGLChartRenderer.DrawHighlights`.
- [x] Шаг 8: Интеграция фрейм-листенеров в класс `TOglChart` в `uoglchart.pas`, маршрутизация событий мыши (клик, движение, колесо) и клавиатуры.
- [x] Шаг 9: Добавление тестового примера использования фрейм-листенеров (`TTestLogListener`) в `unit1.pas` и проверка сборки.

- [x] Шаг 10: Выделение всего текста при двойном клике и замена при вводе символов.
- [x] Шаг 11: Изменение курсора на границах выделенной страницы на crSizeWE / crSizeNS и изменение размеров границ (resize).
- [x] Шаг 12: Изменение курсора на crSizeAll с зажатым Ctrl над страницами и перетаскивание страниц (move).

- [x] Шаг 13: Ограничение вывода меток осей и сетки границами страницы с помощью GL_SCISSOR_TEST.

- [x] Шаг 14: Реализация примагничивания границ страниц при изменении размеров и перемещении (чувствительность 5 пикселей по умолчанию).

- [x] Шаг 15: Реализация выделения рамки и зума по региону (при перетаскивании с Ctrl вправо-вниз) и сброса масштаба (при перетаскивании влево-вверх).

- [x] Шаг 16: Добавление свойства `Locked: Boolean` в `cMoveObj` (`uOglChartDrawObj.pas`).
- [x] Шаг 17: Интеграция `Locked` в слушатели и hit-test рендерера (`uOglChartFrameListener.pas`, `uOglChartRenderer.pas`).
- [x] Шаг 18: Перевод перемещения страниц с клавиши `Ctrl` на `Shift` в `TChartPanZoomListener` (`uOglChartFrameListener.pas`).
- [x] Шаг 19: Добавление отступов (padding) и подложек для меток, уменьшение толщины рамок выделения оси (`uOglChartRenderer.pas`).
- [x] Шаг 20: Динамическое смещение осей Y по горизонтали и переключение шрифтов при выделении (`uOglChartRenderer.pas`).
- [x] Шаг 21: Написание тестов для новых фич (блокировка Locked, смещение и выделение осей Y).
- [x] Шаг 22: Добавление 20% запаса по границам (по 10% с каждой стороны) при выполнении полного масштаба (FitPageZoom).
- [x] Шаг 23: Добавление TSplitter и TStatusBar на форму тестового приложения (`unit1.lfm`, `unit1.pas`).
- [x] Шаг 24: Вывод координат мыши (оконные чарта, оконные страницы, масштабные по выбранной оси) на статусбар с поддержкой NaN (`unit1.pas`).
- [x] Шаг 25: Добавление второй Y-оси и перевод одной из серий на неё в `CreateTestChart` (`unit1.pas`).
- [x] Шаг 26: Исправление закрашивания текста меток в `DrawHighlights` (перерисовка поверх FillRect) (`uOglChartRenderer.pas`).
- [x] Шаг 27: Исправление отображения осей в многоосевом режиме (динамический расчет отступа левой границы сетки в `PageContentRect`) (`uOglChartRenderer.pas`).

- [x] Шаг 28: Перенос математики кубических сплайнов и Безье-интерполяции из `u2DMath.pas`.
- [x] Шаг 29: Реализация вершин Безье (`cBeziePoint`) и сглаживания сплайнами в `cTrend` (`uOglChartTrend.pas`).
- [x] Шаг 30: Хит-тестирование вершин тренда и оси Y при клике левой кнопкой мыши (`uOglChartRenderer.pas`).
- [x] Шаг 31: Поддержка интерактивного перетаскивания (drag-and-drop) вершин тренда в `TChartPanZoomListener` (`uOglChartFrameListener.pas`).
- [x] Шаг 32: Настройка демонстрационного Безье-тренда и интеграция выбора осей с выводом координат в статусбар (`unit1.pas`).

- [x] Шаг 33: Перенос `u2DMath.pas` в специализированный каталог `math` (`SharedUtils\math\u2DMath.pas`) и обновление путей поиска в `project1.lpi`.
- [x] Шаг 34: Устранение Access Violation в `cBaseObj.GetChildCount` при клике путем своевременной инициализации `lModel` в `TChartSelectListener.MouseDown`.
- [x] Шаг 35: Добавление измерения FPS и времени рендеринга с использованием системного счетчика `QueryPerformanceCounter` в `TOglChart.Paint`.
- [x] Шаг 36: Реализация события `OnAfterRender` в `TOglChart` и интеграция его в `unit1.pas` для отображения FPS в нижней панели `StatusBar1`.

- [x] Шаг 37: Исправление кликабельности вершин тренда (обход через оси Y в TChartSelectListener и TChartPanZoomListener).
- [x] Шаг 38: Разделение осей Y по горизонтали с расчетом динамического отступа по ширине меток и отрисовка их собственными цветами.
- [x] Шаг 39: Снижение толщины линий рамок выделения оси до 1.2/1.5 для более аккуратного вида.
- [x] Шаг 40: Исправление багов перетаскивания вершин Безье в `TChartPanZoomListener.MouseMove` (удаление ошибочного прерывания `fIsDraggingPoint := False`).
- [x] Шаг 41: Добавление переопределения `KeyPress` в `TChartSelectListener` для изменения типа интерполяции выделенной вершины тренда (клавиши `1` - Corner, `2` - Smooth, `3` - Null, `T`/`t` - циклический перебор).
- [x] Шаг 42: Реализация отрисовки различных форм вершин Безье (квадрат для bptCorner, ромб для bptSmooth, треугольник для bptNull) в `TOpenGLChartRenderer.DrawTrendPoints`.
- [x] Шаг 43: Реализация отрисовки касательных векторов Безье (gray-линии и серые маркеры в точках Left/Right) для выделенных сглаженных (bptSmooth) вершин в `TOpenGLChartRenderer.DrawTrendPoints`.
- [x] Шаг 44: Реализация интерактивного перетаскивания касательных векторов Безье (зеленые маркеры) с обновлением `UpdateBezieLeft`/`UpdateBezieRight` и логикой следования усов за вершиной в `uOglChartTrend.pas`.
- [x] Шаг 45: Реализация методов `InsertBeziePoint` (поиск правильного индекса по X для сохранения сортировки) и `DeleteBeziePoint` в `cTrend` (`uOglChartTrend.pas`).
- [x] Шаг 46: Переопределение `KeyDown` в `TChartSelectListener` (`uOglChartFrameListener.pas`) для вставки новых вершин (по клавише Insert) под курсором мыши и удаления выделенной вершины (по клавише Delete).
- [x] Шаг 47: Перевод получения координат мыши для клавиши `Insert` в `KeyDown` на системный вызов `Mouse.CursorPos` с трансляцией через `ScreenToClient` для обеспечения 100% точности.
- [x] Шаг 48: Умное поведение клика по пустому месту в `TChartSelectListener.MouseDown` — снятие выделения со всех вершин выделенного тренда с сохранением выделения самого тренда, чтобы позволить вставку в любом месте.
- [x] Шаг 49: Вывод имени и Caption выделенного объекта графика в панель статуса `StatusBar1` тестового приложения в `UpdateStatusBar` (`unit1.pas`).
- [x] Шаг 50: Интеграция локальных процедур логирования `LogToFile` в `uOglChartFrameListener.pas` и `uoglchart.pas` для вывода событий мыши и клавиатуры в файл `chart_events.log`.
- [x] Шаг 51: Исправлена критическая ошибка отсутствия сброса `fIsDraggingPoint := False` в методе `TChartPanZoomListener.MouseUp`, что приводило к паразитному смещению вершин после их удаления или отпускания кнопки мыши.
