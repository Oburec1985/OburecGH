2\* пиши на русском;

* отвечай кратко, инструкциями без особых описаний если не просят
* если я прошу теб выполнить задание с номером, это значит что задание лежит в файле tz.TXT
  **Память**
* каждую итерацию взаимодействия сохраняй чтоб потом вспомнить на чем остаановилась работа с последним проектом в папке cach

### Комментарии и кодировка Delphi-файлов
1. Все `.pas` и текстовые `.dfm` в этом проекте сохранять в `Windows-1251` (`cp1251`), без `BOM`, с переводами строк `CRLF`.
2. Русские комментарии в Delphi-модулях разрешены и предпочтительны там, где они уже приняты в проекте.
   Если меняется существующий файл с русскими комментариями, нельзя переводить его в `UTF-8`.
3. После любых автоматических правок файлов `.pas/.dfm` нужно отдельно проверить, что русские комментарии читаются именно как `cp1251`, а не превратились в `????`, `Р…`, `Ð…`, `Ñ…`, `T…`, 
и другие признаки битой перекодировки.
4. Если есть риск повредить кодировку, лучше:
   - править файл инструментом, который явно пишет `Windows-1251`
   - или не трогать комментарии вообще, если задача не требует их менять
5. Нельзя массово пересохранять Delphi-файлы через инструменты, которые по умолчанию пишут `UTF-8`, если не задана явная кодировка `cp1251`.
6. После правок Delphi-файла обязательно делать быструю проверку:
   - файл открывается как обычный русский текст
   - комментарии читаемы
   - нет лишних пустых строк между каждой строкой кода
   - нет `BOM`
   - формат строк `CRLF`
7. Если комментарий был повреждён, его нужно восстановить сразу в исходном файле, а не оставлять ASCII-заглушки без необходимости.
8. Для новых комментариев:
   - в существующих Delphi-файлах с русскими комментариями писать комментарии по-русски
   - сохранять их только в `Windows-1251`
   - не смешивать в одном файле хаотично `UTF-8` и `cp1251`

**Работа с кодом**

* пишем проект на Delphi2010;
* не сломай кодировку коментов с кирилицей! win-1251;
* прежде чем что-то сделать предложи план доработок. Старайся дробить задачи так, чтоб каждый пункт можно было выполнить и проверить по отдельности;
* пиши тесты для добавленных фич (спрашивай в какой файл их добавить); Тест должен выполнять 2 задачи: тест корректности работы, пример использования кода.
* прежде чем что-то доработать проанализируй - не сломаешь ли повеждение других модулей программы и не делаешь ли ты дублирующий функционал (старайся пользоваться существующими функциями);
* именование переменных: локальные переменные с префиксом l, например lnum, глобальные с префиксом g\_, например g\_DBase, поля классов защищенные с f fProperty, члены классов с доступом m\_,
* перекомпиляцию проекта я буду вызывать сам
* Если освобождаешь память при старт/стопах или загрузке и т.д. старайся не пересоздавать объекты, а очищать данные в них. Предусматривай методы clear, т.к.
  ссылки на объекты могут хранится в других объектах программы и я не хочу испортить эти ссылки.

**Структура каталогов**

* в папке docs документация/ задания/ схемы проекта;
* папку cach веди самостоятельно. Файл plan.md - здесь веди план текущих доработок. Отмечай выполненые задачи. В файлах notes\_xxx.md - веди заметки и выводы по решаемым
  задачам так, чтобы вслучае прерывания работы ты вспомнил на чем остановился; Каталог cach создавай индивидуально для каждого проекта
* если требуется анализировать pdf файлы и картинки, сохраняй результата анализа в файлы notes\_<имя файла>.md чтобы при повторном анализе не надо было тратить время на разбор
  исходного файла;
* каталог sharedUtils - общий каталог с библиотеками используемыми в разных проектах/ Содержит подкаталоги: math (математика), lists (списки), клмпоненты и т.д.;
* при изменении кода корректируй документацию;
* Заданиея написаны в файле tz.md в каталоге docs/TZ

**MCP серверы**

* Работа с Excel: ExcelMcpPrj.exe - скомпилированный файл проекта для работы с Excel (MCP сервер, не классический,
  принцип  работы отличается). Его подключение не прописано в settings.json т.к. это не классический mcp.
  Для понимания его работы пользуйся tools\_ru.md
* Context7 (пока не подключен)

**Многопоточное программирование**

* состояние потока (например, play/suspend) менять только из самого потока!!! Из вне можно дать команду - останавливайся, стартуй и т.д.
* В RunTime (в потоке) - память не выделять!!!! Выделять память только заранее по априорным данным.
* Старайся реже использовать механизм Event - лучше использовать критические секции. Механизм Event труднее в отладке (с поправкой на кроссплатформу, я пишу в контексте винды);
* Не правь мои установки функции suspend (я знаю что она не безопасна, но использую ее правильно)
