unit uSyncThread;

interface

uses
  Classes;

type
  cSyncThread = class(TThread)
  private
    procedure doMainThread;
  protected
    procedure Execute; override;
  public
    constructor create;override;
    destructor destroy;override;
  end;

implementation

uses
  uWpExtPack;


constructor cSyncThread.create;
begin
  // false - автозапуск потока
  inherited create(false);
  FreeOnTerminate:=true;
end;

destructor cSyncThread.destroy;
begin
  inherited;
end;

procedure cSyncThread.doMainThread;
begin
  extPack.CreateFrm;
end;

procedure cSyncThread.Execute;
begin
  Synchronize(doMainThread);
  Terminate;
end;

end.
