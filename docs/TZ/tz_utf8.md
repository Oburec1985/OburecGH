Пишем тестовый проект для работы с ЦАП (tests/TestDAC). При выполнении пунктов ТЗ отмечай статус по каждому пункту так, чтобы при следующей сессии после того как прочитаешь файл ты вспомнил контекст  1. Проанализируй код проекта опиши принцип действия в файле arch.md (каталог cach) **Статус: ? Выполнено** - Создан файл `cach/arch.md` - Описана структура модулей: Unit1.pas, uDacDevice.pas, uSoundCardDac.pas - Описана архитектура: TBlockQueue, TDataGeneratorThread, TDacDevice - Описан принцип работы с waveOut API и callback-модель - Описаны режимы генерации (Sin, SweepSin) и двойная буферизация  Задание 2. Статус: ? Выполнено При запуске ЦАП потом останове, потом следующем запуске происходит ошибка raise Exception.Create('Error writing to waveOut device: ' + IntToStr(ResultCode)); (uSoundCardDAC) Давай исправим  **Решение:** 1. Добавлен сброс `FQueuedBuffers := 0` в методе `Start` перед заполнением очереди 2. Добавлен сброс `FQueuedBuffers := 0` в методе `Stop` для остановки постановки новых буферов 3. В `QueueBuffer` изменена логика - при отсутствии свободных буферов теперь `exit` вместо исключения 4. Флаги цикла (`WHDR_BEGINLOOP`, `WHDR_ENDLOOP`) устанавливаются **после** `waveOutPrepareHeader` 5. В `Open` инициализируется `FCurrentLoopCount := 1` по умолчанию 6. Callback проверяет `FDeviceHandle = 0` для защиты от гонок при закрытии 7. Добавлен метод `TBlockQueue.GetBlockIndex` для получения индекса блока по указателю 8. Добавлен метод `TDacDevice.GetBlockIndex` для доступа из наследников  **Причина проблемы:** - Счетчик буферов `FQueuedBuffers` не сбрасывался перед каждым запуском - Флаги цикла устанавливались до `waveOutPrepareHeader`, что вызывало ошибку 33 (MMSYSERR_INVALPARAM) - При повторном запуске из состояния `stOpened` использовались старые буферы без очистки - Очередь блоков не очищалась между запусками  **Итог:** ЦАП теперь корректно работает в режиме: запуск ? останов ? запуск (многократно)  Задание 3. Стутус: **Задача: Создание модуля управления сценариями ЦАП (uDACProgram.pas) на Delphi. Контекст:У меня есть система вывода звука (uDacDevice, uSoundCardDac), работающая через waveOut. Мне нужно вынести логику формирования сигналов из UI (Unit1) в отдельную иерархию классов «Программ».  **Архитектурные требования: 3.1. Базовый класс TDacProgram: - Содержит ссылку на fDevice: TDacDevice. - Хранит fCurrentPhase: Double (для обеспечения преемственности фазы между буферами). - Имеет виртуальный метод ProcessBuffer(P: Pointer; Size: Integer), который вызывается внутри обработчика OnBufferEnd. - Содержит метод для расчета «выравнивания» буфера под частоту (GetAlignedSize(Freq: Double; MinSamples: Integer): Integer.  3.2. Наследник TSimpleSinusProgram (Тупое управление): - Реализует простую выдачу синуса по текущим параметрам Frequency и Amplitude. - Важно: Сохраняет текущую фазу, чтобы не было разрывов внутри одного потока, но НЕ меняет размер буфера устройства (использует текущий fDevice.BufferSize). Это «базовое поведение» без подстройки под кратность периодов.  3.3. Наследник TAccurateSinusProgram (Умное управление): - При изменении частоты автоматически вычисляет новый размер буфера, кратный периоду (используя MinSamplesTarget). - Вызывает fDevice.ReallocateBuffers(NewSize), если это необходимо. - Гарантирует, что в буфере всегда целое число периодов.  **Технические детали для реализации: - Модуль должен использовать типы из uCommonTypes (например, PSmallIntArray). - Реализуй механизм подписки программы на события устройства: программа должна сама назначать свой метод на fDevice.OnBufferEnd. - Предусмотри защиту: если частота $0$ или слишком мала, размер буфера не должен превышать разумные пределы, или переходи на логику с тупым поведеним когда надо склеивать несколько буферов. - Напиши пример кода для Unit1, как инициализировать программу и переключаться между TSimpleSinusProgram и TAccurateSinusProgram.  **Ожидаемый результат: Полный исходный код модуля uDACProgram.pas и пример интеграции в Unit1.  Задание 4. Стутус: uDacDevice.pas — базовый абстрактный класс. uSoundCardDac.pas — реализация через WinAPI waveOut с использованием очереди блоков (FBlockQueue). Unit1.pas — клиентская логика (UI и генерация сигналов).  Задача:  Необходимо реализовать механизм «кратности периодов», чтобы при зависании программы звуковая карта могла бесконечно и без щелчков воспроизводила последний буфер. Если я включил воспроизведение и потом меняю частоту опроса старые буферы в очереди доигрываются, генерится новый буфер с кратной памятью и ставится в очередь воспроизведения так, чтобы ЦАП продолжил новую частоту без щелчков. Обрати внимание, что генерация в Unit1 идет не с помощью зацикливания в реально передаются постоянно новые блоки  - это сделано специально чтобы когда я захочу воспроизводить циклограмму частот (Step Sinus) - это сработало  Требования к реализации:   Разделение ответственности: Параметры сигнала (частота, амплитуда) и расчет кратности периодов должны находиться только в Unit1 (логика клиента). TDacDevice и его наследники должны отвечать только за транспорт и управление памятью. Параметризация: Вместо фиксированных констант (типа 1024), расчет размера буфера должен основываться на переменной MinSamplesTarget, передаваемой из клиента.  Динамический ресайз: В TSoundCardDac нужно реализовать метод ReallocateBuffers(NewSize: Integer), который: - Останавливает воспроизведение (waveOutReset). - Очищает очередь FBlockQueue. - Освобождает старую память через waveOutUnprepareHeader и GlobalFree. - Выделяет новую память под NewSize и готовит заголовки WAVEHDR.  Непрерывность фазы: В клиенте (Unit1) реализовать хранение текущей фазы (fCurrentPhase), чтобы при смене частоты или переходе между буферами синусоида оставалась непрерывной, ИЛИ сделать так чтобы память выделялась под кратные периоды синусойд которые всегда заказнчивались нулем.  Алгоритм в Unit1: Напиши функцию, которая на входе принимает Frequency и MinSamplesTarget, а на выходе дает BufferSize, кратный целому числу периодов для текущей SampleRate.