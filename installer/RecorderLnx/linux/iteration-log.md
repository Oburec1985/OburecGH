# Журнал итераций Linux-установщика RecorderLnx

## 2026-09-11 18:35 — deploy восстановлен и выполнен на четырёх ПК

**Запрос:** выяснить причину подозрительно быстрого завершения deploy и
установить готовый `recorderlnx_0.1.5_amd64.deb` на весь список.

**Сделано:** подтверждено, что CMD разрывал удалённую команду на символах `|`;
команда переписана без внутренних pipe. Пароль берётся из inventory через
рабочий временный askpass EXE. Выполнен повторный массовый deploy.

**Проверка:** `.66`, `.85`, `.45`, `.17` — package 0.1.5, правильный BaseUrl,
HostAgent отвечает; итог BAT: 4 succeeded, 0 failed.

**Статус:** готово.

## 2026-09-11 — Защищённое массовое SSH-обновление

**Запрос:** запускать одним BAT переустановку свежего DEB RecorderLnx на четырёх Linux-ПК.

**Сделано:** добавлены deploy BAT и отдельный inventory; перед сборкой и подключениями BAT требует ровно четыре активных SSH-адреса. В inventory внесены подтверждённые `192.168.9.66` и `192.168.9.85`, пароли не сохраняются.

**Проверка:** fail-fast сценарий выполнен: при двух адресах BAT завершился с кодом 1 до сборки и изменений удалённых ПК.

**Статус:** частично; нужны ещё два SSH-адреса, после чего можно выполнить стендовое развёртывание.

## 2026-09-11 — Единая BAT-подготовка rcPanel и HostAgent

**Запрос:** проверить, что BAT для флешки подкладывает актуальные rcPanel и
RecorderHostAgent.

**Сделано:** BAT теперь проверяет свежесть обоих Linux ELF, обновляет rcPanel,
пересобирает DEB с HostAgent, копирует его в payload и побайтно сверяет копию.
Старый ELF приводит к ошибке вместо ложного `READY`.

**Проверка:** BAT корректно остановился на текущих устаревших Linux-сборках
rcPanel и HostAgent; обе проверки называют конкретный более новый исходник.

**Статус:** скрипт готов; перед получением `READY` нужно пересобрать оба ELF
на Linux и снова выполнить BAT.

## 2026-09-11 — Входящий порт HostAgent

**Запрос:** обеспечить удалённый запуск RecorderLnx через Linux HostAgent.

**Сделано:** postinst разрешает TCP 8766 в активном `ufw` либо `firewalld`;
README дополнен удалённой проверкой и исправленным путём Linux ELF агента.

**Проверка:** Python compile — OK; штатный `build-installer.bat` создал DEB,
SHA-256 `D17494725DFB09B92D63FD4283FFFBE4E27BD661E6DEB900D53F0DAA73BA0D92`.
Фактический удалённый запуск требует повторной установки и проверки на стенде.

**Статус:** пакет готов; стендовая проверка остаётся.

## 2026-09-11 11:59 — автозапуск HostAgent в Astra

**Запрос:** Встроить в Linux DEB реально работающий автозапуск RecorderHostAgent.

**Сделано:** добавлен системный XDG Autostart-файл; прежняя глобальная systemd-регистрация отключается при обновлении, чтобы два агента не занимали TCP 8766; install-check и README актуализированы.

**Проверка:** `build-installer.bat` завершён успешно; в готовом DEB проверены postinst и `/etc/xdg/autostart/recorder-host-agent.desktop`; SHA-256 `BA4DA15620E8BBD71ED11A0A05A6999EDFF9B6B16DF2906FF98884ED848ED56D`.

**Статус:** готово; после установки нужен новый графический вход для автоматического старта в уже открытой сессии.

## 2026-09-11 11:34 — DEB из прямого HostAgent output

**Запрос:** Собрать DEB после проверки нового Target Filename без возврата sync-схемы.

**Сделано:** штатный `build-installer.bat` упаковал agent непосредственно из output RecorderLnx.

**Проверка:** DEB создан `11:33:32`, размер `17640850`, SHA-256 `968771A5D5ECF754497E88C63A7B33AB923878DDB7E717C108254C0700D1B811`.

**Статус:** готово.

## 2026-09-11 11:27 — DEB с новым TryStart agent

**Запрос:** Переупаковать Linux installer после актуальной сборки HostAgent.

**Сделано:** новый ELF синхронизирован в output RecorderLnx и включён в DEB.

**Проверка:** DEB создан `2026-09-11 11:26:31`, SHA-256 `9FC6D7F6ACBA6DA5857C089586B2E5B458E03FE2E0B07E6AB16DFDDB5EF1C43E`; упаковщик подтвердил SHA-256 agent `B9AD0B8659FB5DFE9ECBEB63D03688C695551F7F291E6F582476B819B92E7269`.

**Статус:** готово.

## 2026-09-11 11:23 — agent из соседнего каталога сборки

**Запрос:** Брать Linux launcher из того же output-каталога, где лежит RecorderLnx.

**Сделано:** build-installer синхронизирует ELF из проекта agent в `RecorderLnx/lib/x86_64-linux`; build_deb упаковывает только соседний ELF.

**Проверка:** DEB пересобран, SHA-256 `FBB93023E58334FCB012C3CFFDD721B70C3ECD250DD03B68A7C60115E2F58194`.

**Статус:** готово.

## 2026-09-11 11:19 — немедленный запуск RecorderHostAgent

**Запрос:** Устранить молчаливое отсутствие launcher после установки Linux-пакета.

**Сделано:** postinst после глобального enable выполняет daemon-reload и enable --now в активных user-systemd сессиях; результат пишет в install.log. В unit включено явное журналирование.

**Проверка:** Python builder скомпилирован; `build-installer.bat` создал новый `recorderlnx_0.1.0_amd64.deb`, SHA-256 `40D93656490F9DB7BF3D7B37AC40E7251F2A543D48145220294816F25BB9739A`.

**Статус:** готово к проверке на Linux.

## 2026-09-11 11:35 — Актуальные Linux-инсталляторы

**Запрос:** проверить и пересобрать установочные комплекты всех Linux-программ.

**Сделано:** `build-installer.bat` собрал DEB с актуальными RecorderLnx и
RecorderHostAgent; offline-комплект Firebird/rcPanel обновлён отдельным штатным
`update-offline-installer.bat`.

**Проверка:** обе команды завершились с exit code 0; DEB создан, SHA-256
`964D9E6924B92A52B7273D43BB75ECFCE4AD359B53C78C6CEDD3F07ADABE2E3D`;
rcPanel payload обновлён и проверен скриптом.

**Статус:** готово для копирования установочных каталогов на флешку.

## 2026-09-11 11:30 — RecorderHostAgent в общем DEB

**Запрос:** Устанавливать Linux RecorderHostAgent вместе с RecorderLnx и
автоматически запускать его так, чтобы внешняя команда могла открыть GUI
RecorderLnx в пользовательском сеансе.

**Сделано:** DEB-сборщик включает Linux ELF агента, его INI и глобально
включаемый user-systemd unit `graphical-session.target`; добавлены проверки
наличия и побайтового совпадения агента в пакете. Windows не изменялся.

**Проверка:** `py_compile` и `sh -n` для `postinst`/`postrm` — OK. Полная
сборка штатно остановилась до staging: Linux ELF RecorderHostAgent ещё не
собран по ожидаемому пути.

**Статус:** частично; собрать `RecorderHostAgent.lpi` на Linux и повторить
`build-installer.ps1`.

## 2026-09-11 — Массовое обновление четырёх Linux-компьютеров

**Запрос:** подготовить один BAT, который собирает свежий DEB и по SSH
переустанавливает RecorderLnx на заданном списке из четырёх компьютеров.

**Сделано:** добавлен `deploy-linux-all.bat` с отдельным конфигурационным
списком адресов. Для каждого узла пакет копируется под уникальным именем в
`/tmp`, устанавливается через `sudo dpkg`, при подтверждённой нехватке
зависимостей выполняется `apt-get -f`, затем проверяются версия пакета,
исполняемые файлы, XDG-autostart и локальный статус HostAgent. Ошибка одного
узла не останавливает обновление остальных; в конце выводится сводка.

**Проверка:** проверена fail-fast ветка отсутствующего списка хостов. Реальное
развёртывание не запускалось: адреса четырёх компьютеров намеренно не угаданы.

**Статус:** BAT готов; требуется заполнить `deploy-linux-hosts.txt` и запустить.
# Журнал итераций Linux-установщика RecorderLnx

## 2026-09-11 18:20 — deploy использует готовый DEB и пароли списка

**Запрос:** восстановить проход `deploy-linux-all.bat` по всем хостам и
устанавливать последний готовый DEB из `linux\Output` без пересборки.

**Сделано:** удалён вызов сборщика; выбирается самый свежий
`recorderlnx_*_amd64.deb`. Неработоспособный `.cmd` SSH_ASKPASS заменён
временным EXE; пароль по-прежнему читается из строки `user@host|password`.

**Проверка:** helper вернул тестовое значение — OK; prepare-only выбрал
`recorderlnx_0.1.5_amd64.deb`, удалённые хосты не изменялись.

**Статус:** готово к массовой установке.

## 2026-09-11 — Версия пакета для централизованной SQL-конфигурации

**Запрос:** доставить исправление единого подключения Firebird в RecorderLnx.

**Сделано:** версия Linux-пакета по умолчанию повышена до 0.1.6; шаблон
`sql-db.ini` использует сервер 192.168.9.66 и серверный путь
`/var/opt/mera/SQLdb/recorderlnx.fdb`. Секрет не встраивается в пакет: его
заполняет установщик Firebird и затем рассылает RCPanel из рабочего конфига.

**Статус:** требуется Linux-сборка RecorderLnx 0.1.6 и упаковка DEB.

## 2026-09-11 — Синхронизация SYSDBA

**Запрос:** гарантировать одинаковый пароль Firebird на сервере и клиентах.

**Сделано:** установщик меняет реальный пароль SYSDBA на `123`, обновляет
`SYSDBA.password` и записывает тот же пароль вместе с адресом, портом и сетевым
путём БД в рабочий `sql-db.ini`. Версия пакета повышена до 0.1.7.

## 2026-09-14 — NetworkShareManager в Linux-инсталляторе

**Запрос:** включить утилиту сетевых ресурсов в пакет RecorderLnx и устранить
ложное сообщение, что основной бинарник устарел из-за исходников утилиты.

**Сделано:** в DEB добавлены Linux ELF, SMB-helper и ярлык; добавлены зависимости
`cifs-utils` и PolicyKit. Каталог `Tools` исключён из проверки основного
RecorderLnx, для NetworkShareManager сделана отдельная проверка свежести.

**Проверено:** `py_compile` успешен; `build-installer.bat` завершился с кодом 0
и создал `recorderlnx_0.1.10_amd64.deb`.

## 2026-09-14 — Сетевые пути SQL-трендов

**Запрос:** после публикации произвольного каталога замеров открывать из SQL
замеры другого Linux-ПК независимо от исходного абсолютного пути.

**Сделано:** добавлен кроссплатформенный resolver. Сначала используется прямой
путь или `smb://`; на Linux затем проверяются подключённые ресурсы из
`/etc/recorderlnx/network-shares.d` по хвосту `usml/...` либо
`<каталог кадра>/<файл.mera>`. В пакет включён helper публикации и Samba.
Для старых Astra добавлен автоматический fallback SMB 3.0 → 2.1.

**Проверено:** RecorderLnx принудительно собран в Astra VM с кодом 0; пакет
собран с кодом 0; на KIP-4 опубликован `MeraFiles`, на KIP-3 ресурс смонтирован,
файл `0031/0031.mera` доступен по локальному пути менеджера.

## 2026-09-14 — Общая утилита настройки Linux

**Запрос:** объединить менеджер сетевых ресурсов с расширяемой утилитой
настройки Linux и добавить переименование ПК, WOL и публикацию каталогов.

**Сделано:** создан `LinuxSetupManager` со списком функций и контекстной
справкой, включая консольные эквиваленты. Добавлены функции имени ПК,
Wake-on-LAN для `enp3s0`, публикации `MeraFiles` и подключения SMB. Отдельный
`NetworkShareManager` удалён из группы и пакета. Публикация сохраняется в fstab.
В корне OburecGH создано проектное правило параллельной работы агентов.

**Проверено:** LinuxSetupManager принудительно собран в Astra VM с кодом 0;
финальный DEB собран с кодом 0 и установлен на KIP-3 с кодом 0. Проверено наличие
новой утилиты и удаление старого NetworkShareManager.
## 2026-09-14 — CLI, именованные SMB-ресурсы и восстановление SQL-путей

**Запрос:** сохранить GUI утилиты настройки Linux, добавить CLI, исправить
публикацию и отключение SMB-ресурсов, открывать по double-click один замер и
восстанавливать недоступные пути через выбранный пользователем каталог.

**Сделано:** добавлен headless `LinuxSetupManagerCli` и launcher
`recorderlnx-linux-setup`; публикация принимает имя ресурса, Recorder хранит его
в `[Record] ShareName` и отправляет `smb://host/share/...` в lifecycle-событии.
Менеджер читает подключения из INI-реестра, при выборе показывает `//host/share`,
а helper отключения поддерживает занятые mount points и удаляет точную запись.
Double-click открывает только текущую строку. При недоступном пути пользователь
выбирает каталог; обновляются только найденные битые пути текущего события и
только для хоста выбранного замера.

**Проверено:** GUI-менеджер, CLI и RecorderLnx принудительно собраны Lazarus
2.2.6/FPC 3.2.2 в Astra VM; CLI проверен без `DISPLAY`; shell helper-ы прошли
`bash -n`. Финальный `recorderlnx_0.1.10_amd64.deb` собран, SHA256
`1535E3D13DD27BFC61A3746489F32D1E77458C191C83C4541E682ECC872C02D1`, и
успешно установлен на `192.168.9.17`, `.45`, `.66`, `.85`.

**Осталось:** `192.168.9.92` недоступен по SSH. `192.168.9.200` исключён по
указанию пользователя; предыдущая попытка оставила пакет в состоянии `unpacked`
из-за отсутствующего `cifs-utils` и недоступного репозитория.
