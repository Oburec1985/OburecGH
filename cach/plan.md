# План работ: TSpinButtonEx

## Текущая задача
Добавление нового компонента `TSpinButtonEx` (наследник `TSpinButton`) с фильтрацией дребезга кликов.

## Шаги
- [x] Подготовка `Spin.pas`: перевод приватных полей и методов в `protected`, добавление `virtual` к `BtnClick`.
- [x] Создание модуля `uSpinEx.pas` с классом `TSpinButtonEx`.
- [x] Реализация свойства `ClickDelay` и логики фильтрации (по умолчанию 1000 мс).
- [x] Регистрация компонента в `DCL_MYOWN.pas`.
- [x] Добавление модуля в проект пакета `dcl_own.dpk`.
- [x] Проверка записи (тестовые файлы удалены).
